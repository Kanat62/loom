# DECISIONS — журнал решений в неопределённостях

Формат: **[Фаза] решение** — почему, где в коде. Документы (ТЗ v4, DEV_GUIDE) —
единственный источник правды; здесь фиксируются только те развилки, которые
документы не решают напрямую.

---

## Фаза 0

- **DB-адаптер: try/catch require('better-sqlite3'), fallback node:sqlite** —
  ТЗ §20.8 требует именно эту пару без деталей выбора момента проверки. Выбрал
  ленивую загрузку в `db.js:pickDriver()` (проверяется при первом `openDb()`,
  не при импорте модуля) — чтобы одна недоступная сборка не валила импорт
  `journal.js` целиком. better-sqlite3 на этой машине собрался нормально
  (Windows, Node 22.23.1) — оба драйвера проверены `evals/race.js`.
- **journal.db: WAL + busy_timeout=5000 на обоих драйверах** — ТЗ требует
  BEGIN IMMEDIATE для atomic capture; WAL снижает вероятность SQLITE_BUSY при
  параллельных воркерах (Фаза 6), busy_timeout — подстраховка вместо ручного
  retry-цикла на уровне журнала.
- **root_spec: ключ project_id (default 'default'), а не глобальный singleton**
  — ТЗ §4 описывает root_spec как факт, не даёт схему. Сделал таблицу с PK
  project_id, чтобы не блокировать многопроектность в будущем без миграции,
  при этом v1 всегда использует 'default'. `journal.js: setRootSpec/getRootSpec`.
- **skills.js: hallucinationGuard — regex по чужим расширениям + проверка,
  что файловые токены урока входят в touches_files задачи** — §4 описывает
  правило словами ("ссылается только на реальные файлы задачи"), конкретный
  алгоритм не дан. Реализовал как чистую функцию `skills.js:hallucinationGuard`,
  используется из `agents/skill.js` (Фаза 3) и из `writeSkill()` напрямую.
- **io.js: sanitizeLine здесь, а не в talk.js** — шрам 32 про санитизацию
  ввода сеанса привязан к слою чтения строк, а не к конкретной точке входа;
  так переиспользуется всеми будущими REPL (talk.js, resume.js).
- **mock.js в Фазе 0 — только каркас (MOCK_HANDLERS пуст, registerMockHandler
  экспортирован)** — агентов ещё нет, реальные ветки появляются вместе с
  ролями (Фаза 1: coder/tester; Фаза 3: advisor/router/architect/skill;
  Фаза 5.5: live_in), как явно указано в DEV_GUIDE §2.
- **package.json: better-sqlite3 в optionalDependencies, не dependencies** —
  чтобы `npm install` не падал целиком на машинах без toolchain для нативной
  сборки; адаптер и так рассчитан на отсутствие пакета.
- Два маленьких реальных вызова `claude -p --model haiku ...` (~$0.004
  суммарно) сделаны ДО фазы 1 для проверки недокументированных флагов CLI
  (`--system-prompt-file`, полный набор изоляции) — эти флаги не описаны в
  `claude -p --help`, только упомянуты в тексте `--bare`. Без проверки нельзя
  было доверять контракту шлюза. Дальнейшие реальные прогоны отложены в
  RUNBOOK.md по вашей просьбе.

## Фаза 1

- **Windows: `spawn('claude', args, {shell:true})` + ручное
  `JSON.stringify()`-квотирование каждого аргумента** — обнаружен реальный баг
  при сборке шлюза: прямой `spawn('claude', ...)` без shell даёт `ENOENT`
  (Windows не резолвит `.cmd`-шим по PATH без шелла), `spawn('claude.cmd', ...)`
  без shell даёт `EINVAL` (известная особенность Node на Windows для
  .bat/.cmd), а `shell:true` с аргументами-путями, содержащими пробел, обрезает
  аргумент по первому пробелу без ручного квотирования. Решение проверено
  вживую (см. `gateway.js:buildSpawnArgs`). На POSIX — обычный `spawn` без
  shell, без кавычек (кавычки на POSIX испортили бы аргумент, там шелл не
  участвует). Это и есть требуемая инвариантом §20.6 «Windows-ветка для
  claude.cmd», просто конкретный механизм не был описан в документах.
- **checkRunner: shell-обвязка критериев — всегда `bash -c`, включая Windows**
  — `bash` из Git for Windows резолвится напрямую (это настоящий `.exe`, не
  `.cmd`-шим), поэтому кросс-платформенно работает без shell:true и без
  Windows-квотирования. Соответствует прямому указанию DEV_GUIDE §0
  («bash-зависимости checkRunner документированы: Windows = Git Bash»).
- **workspace/package.json создаётся координатором автоматически, БЕЗ поля
  "type"** — реальный прогон MOCK-сценария калькулятора упал: Node ищет
  ближайший package.json вверх по дереву, находил корневой package.json LOOM
  (`"type":"module"`) и трактовал `calc.js` продукта как ESM, из-за чего
  `module.exports` кидал `ReferenceError`. Ни ТЗ, ни DEV_GUIDE это не
  упоминают — фикс в `coordinator.js:ensureWorkspacePackageJson`. LOOM не
  должен навязывать продукту клиента свою модульную систему.
- **Auto-commit safety net в варианте А — безусловный git-коммит харнессом
  после каждого хода кодера, а не «страховка на случай, если кодер забыл
  git_commit»** — в JSON-варианте А у кодера физически нет git-инструмента
  (нет tool_use), поэтому «забыть закоммитить» структурно невозможно; сам
  факт того, что коммитит харнесс, а не модель — и есть реализация принципа
  шрама 4 в этом варианте (§7: полноценный `submit_result`/git_commit —
  спящая спецификация варианта Б). `coordinator.js:autoCommit`.
- **Критерий = `{cmd, expect}` (expect опционален); JS-инлайн распознаётся
  строго по форме `node -e "<код>"` / `node --eval '<код>'` целиком** —
  DEV_GUIDE даёт контракт `{cmd, expect}` без грамматики извлечения. Регэксп
  матчит ТОЛЬКО команду, целиком состоящую из `node -e "..."`; остальные
  команды (`node --check file.js`, готовый `.cjs`, `curl`) идут в bash как
  есть — извлекать нечего, шрам 7 неприменим. `checkRunner.js:NODE_EVAL_RE`.
- **cli.js принимает свободный текст, `--file task.json` или флаги
  `--title/--spec/--criteria/--role/--type/--budget`** — DEV_GUIDE говорит
  только «задача задаётся руками в коде/аргументом», не давая формат. Выбрал
  три пути ввода: быстрый смоук (позиционный текст, без критерия — уйдёт в
  blocked_needs_human, что честно), файловый (для содержательных ручных
  прогонов вроде DoD sum.js — см. RUNBOOK.md), и флаговый.
- **mock.js: различение задач сценария калькулятора по содержимому
  промпта (`/calc\.js/`, `/index\.html/`, наличие секции «Отчёт тестера»),
  а не по глобальному счётчику вызовов** — счётчик вызовов ролей общий на
  процесс и не привязан к конкретной задаче; текстовое распознавание
  устойчиво к порядку обработки очереди координатором.
- **gateway.js: cost_usd берётся из `total_cost_usd` в JSON-конверте
  claude-cli напрямую** (подтверждено реальным вызовом) — резервная формула
  по `TIER_PRICES_USD_PER_1M` используется только если поле вдруг отсутствует.
  Это точнее и проще условной таблицы цен, о которой пишет §13 ТЗ.

## Фаза 2

- **«Ход» для правила дельты (§6.1) = номер попытки (task.attempts) ИМЕННО
  этой задачи**, не глобальный счётчик вызовов кодера. Ход 1 и ход 2 —
  полный снимок, ход 3+ — added/changed/removed относительно снимка,
  показанного на предыдущем ходу ЭТОЙ ЖЕ задачи. Состояние — in-memory
  `Map(task_id → snapshot)` в `agents/coder.js`; при рестарте процесса между
  попытками откатываемся к полному снимку — деградация в сторону БОЛЬШИХ,
  но корректных данных, не баг. Очищается по завершении задачи
  (`clearEyesState`) координатором, чтобы не течь памятью на длинных прогонах.
- **claimNext() больше НЕ инкрементирует attempts; инкремент — отдельная
  `incrementAttempts()`, вызываемая координатором ПОСЛЕ проверки бюджета,
  ПЕРЕД вызовом кодера.** Обнаружено при живой прогонке evals/budget.js:
  изначально attempts инкрементился прямо в захвате (Фаза 1), из-за чего
  задача, остановленная бюджетом ДО первого реального вызова кодера,
  показывала attempts=2 вместо 1 — захват без реальной попытки тратить деньги
  не должен считаться попыткой. Это меняет семантику `claimNext()` из Фазы 1
  (правка сделана в её же файле, не поверх — гонка по-прежнему защищена той
  же BEGIN IMMEDIATE транзакцией, `evals/race.js` зелёный после правки).
- **evals/budget.js: MOCK_CALL_COST_USD = 0.01 (config.js)** — в MOCK-режиме
  каждый вызов роли раньше логировался с cost_usd=0, что делает бюджет-стоп
  непроверяемым без реальных денег. Синтетическая стоимость 0.01 — тот самый
  «игрушечный лимит» из DoD DEV_GUIDE §4, позволяет проверить остановку
  детерминированно и бесплатно.
- **mock.js: coder-хендлер для задач вне сценария калькулятора возвращает
  файл-заглушку вместо throw** — чтобы evals/budget.js (и будущие MOCK-эвалы)
  могли переиспользовать общий MOCK_HANDLERS.coder, не расширяя сам
  сценарий-эталон калькулятора.
- **Проверка prompt-кэша и дельты tokens_in по avg() — отложены в RUNBOOK.md**
  как реальные прогоны (нужны настоящие вызовы claude-cli, MOCK всегда даёт
  tokens_in=0). Механизм логирования (cache_read/cache_creation в
  events.payload) уже на месте с Фазы 1 — RUNBOOK описывает точную команду
  проверки и SQL.

## Фаза 3

- **`\b` в JavaScript-регэкспах не распознаёт кириллицу как `\w`** — реальный
  баг, найденный дважды за эту фазу: (1) в MOCK-роутере классификация tweak
  по `/^поменяй\b/i` не матчилась на «поменяй вывод...» (граница
  буква→пробел не считается word boundary, когда буква кириллическая);
  (2) в `agents/advisor.js:TECH_QUESTION_RE` — «px/мс» не ловилось тем же
  образом, из-за чего код-уровневый guard шрама 15 фактически не работал бы
  на реальных технических вопросах с кириллицей рядом. Фикс — везде, где
  термин может стоять рядом с кириллицей, использовать
  `(?<![\p{L}\d])term(?![\p{L}\d])` с флагом `u` вместо `\b`. Это НЕ описано
  ни в ТЗ, ни в DEV_GUIDE — чисто платформенная особенность JS-регэкспов,
  но напрямую бьёт по шраму 15 (советник спрашивал px), поэтому фиксирую
  здесь как критичный для доверия к guard'у баг.
- **route на ошибку парсинга роутера — буквально `'project'`, хотя это не
  входит в перечень 5 типов входа §1** — DEV_GUIDE §1 прямо пишет
  `route=project`, приоритет у DEV_GUIDE при расхождении с ТЗ. В
  `agents/router.js` 'project' — синоним «безопасный полный цикл»:
  `talk.js` направляет его в тот же `runProjectFlow` с протоколом `wish`
  (запасной, самый нейтральный протокол интервью из трёх).
- **attempts инкрементируется и для type=regression** (в отличие от
  обычных задач, где attempts = число реальных вызовов кодера) — регрессия
  вообще не вызывает кодера (§8.9), но переиспользование того же счётчика
  не создаёт путаницы (регрессия почти всегда решается за 1 «попытку» —
  один прогон tester'а) и избавляет от отдельной ветки в handleAttemptFailure.
- **Регрессионная задача зависит (blocked_by_task_id) только от ПОСЛЕДНЕЙ
  обычной задачи дерева, не от всех** — схема журнала (§4 ТЗ, дословно)
  даёт только ОДНУ колонку зависимости, полноценный fan-in (несколько
  родителей) — явно Фаза 6. Доказано (и проверено `evals/race.js`-подобной
  логикой claimNext), что при строго однопоточной обработке claimNext
  ВСЕГДА берёт самую старую pending-задачу — значит к моменту, когда
  последняя обычная задача дойдёт до done, все более ранние уже в
  терминальном статусе. Для однопоточного Фаза 3 этого достаточно и корректно;
  бросается в глаза как упрощение, которое Фаза 6 обязана заменить настоящим
  fan-in (§16).
- **advisor.js: интервью в MOCK-режиме сразу возвращает `ready`** (кроме
  специального маркера `ТЕСТ_ТЕХ_ВОПРОС` для `evals/interview.js`) — чтобы
  `npm run mock` оставался быстрым и детерминированным; сам механизм
  многоходового интервью (`continueIntake`, предохранитель
  `MAX_INTERVIEW_TURNS`) проверен структурно (используется в `talk.js`),
  но многоходовый диалог с реальными уточняющими вопросами — предмет
  реального прогона (RUNBOOK).
- **mock.js: сопоставление задач сценария — по title из "## Задача", не по
  всему тексту промпта** — обнаружен реальный баг: секция критерия
  (`## Критерий приёмки`) часто содержит имена файлов вроде `calc.js`
  (потому что сама проверка их читает), из-за чего матчинг по всему
  `userMsg` ложно триггерился. Матчинг сузили до строки title.
- **skill_writer вызывается только на статус `done`, не на
  `blocked_needs_human`** — §4 говорит «после каждой завершённой задачи»;
  трактую «завершённая» как «done», а не любой терминальный статус — урок
  из проваленной задачи менее надёжен (причина провала могла быть внешней:
  бюджет, вопрос человеку), и §4 явно называет момент «после done» через
  описание координатора.

## Фаза 4

- **Docker — опциональный слой, включается автодетектом, а не жёсткая
  зависимость.** Демон Docker недоступен на этой машине (`docker info`
  падает, хотя CLI установлен) — по прямому указанию задания: «если docker
  недоступен в среде — реализуй код полностью, проверку оставь в
  RUNBOOK.md». `checkRunner.runCheck()` по умолчанию использует
  `sandbox:'local'` (безопасный, уже проверенный путь); `coordinator.js`
  сам решает через `ensureDockerSandbox()` — собирает образ РОВНО когда
  демон отвечает И сборка успешна, иначе явно передаёт `sandbox:'local'`.
  Ничего не «auto-детектится» неявно внутри checkRunner — иначе состояние
  «демон есть, а образ не собран» тихо ломало бы тестер (docker run без
  образа полез бы тянуть несуществующий тег из registry).
- **Образ workspace пересобирается один раз на вызов `runCoordinatorLoop`
  для данного workspaceDir, не на каждую задачу** — сборка кэшируется
  Docker по слоям (быстро, если package.json не менялся), но правило
  «пакет понадобился посреди дерева → статус «нужен пакет» → пересборка»
  (§12.2) в этой фазе реализовано КАК РУЧНОЙ путь (новый `/resume` заново
  входит в `runCoordinatorLoop` и пересобирает), не как автотриггер на
  каждое изменение `package.json`. Полная реактивность — по потребности,
  не специфицирована жёстко ни в ТЗ, ни в DEV_GUIDE для Фазы 4.
- **architect.js по-прежнему делает `npm install` напрямую на хосте (execFileSync)**
  вдобавок к тому, что тот же `package.json` попадает в Docker-сборку —
  сознательное дублирование: хостовый install остаётся ЕДИНСТВЕННЫМ
  работающим путём, когда Docker недоступен (наш подтверждённый тестами
  случай), а Docker-сборка ставит те же зависимости ВТОРОЙ раз изолированно
  внутри образа. Не конфликтует (разные файловые системы), не убираю
  хостовый install, чтобы не сломать единственный сейчас проверяемый путь.
- **gitGuard проверяет ветку РЕПОЗИТОРИЯ LOOM (ROOT), не workspace** — шрам 5
  буквально про репозиторий LOOM («репо продукта» — отдельная история,
  Фаза 5/GitHub). `coordinator.gitGuard()` — pre-flight в начале
  `runCoordinatorLoop()`, бросает исключение (не тихо продолжает), если
  ветка LOOM не main/master; если ROOT вообще не git-репозиторий — не
  блокирует (нечего защищать, напр. тесты во временных каталогах).
- **tsconfig.json — заготовка, НЕ подключена к координатору** (DEV_GUIDE
  §6: «включается в Фазе 6»). Присутствие файла — чтобы Фаза 6 не начинала
  с нуля; `tsc --noEmit` level-0 нигде не вызывается автоматически сейчас.
- **Реальная проверка `rm -rf /` внутри контейнера не трогает хост —
  отложена в RUNBOOK.md** (нужен работающий демон Docker, недоступен в
  этой сессии). Путь замка (`../evil.js` блокируется и логируется событием
  `path_lock_blocked`) проверен и зелён в MOCK — это чисто харнесс-уровневая
  защита, Docker не нужен для её работы (действует уже с Фазы 1, §12.1
  «до Docker — временная решётка»).

## Фаза 5 (GitHub) — сознательно пропущена

По прямому указанию задания реализуются фазы 0,1,2,3,4,5.5 — Фаза 5
(gh CLI, PR-поток) не строится. `agents/livein.js`/`talk.js` не зависят от
неё; auto-commit safety net (шрам 4) уже работает на git-репозитории
workspace с Фазы 1, этого достаточно для Фазы 5.5.

## Фаза 5.5

- **live_in: Playwright — опциональная зависимость (`optionalDependencies`,
  как better-sqlite3), с фолбэком на статический разбор HTML.** В этой
  среде `npm install playwright` не выполнялся (браузерные бинарники
  тяжёлые, не нужны для проверки MOCK-веткой) — `agents/livein.js:
  inspectWithPlaywright()` пробует `import('playwright')`, при неудаче
  честно откатывается на `inspectStatic()` (читает HTML напрямую,
  помечает результат `live:false` и текстовым `note`, чтобы промпт
  live_in знал о сниженной надёжности данных). Живой Playwright-прогон —
  RUNBOOK-пункт (после `npm install && npx playwright install chromium`).
- **live_in создаёt задачи `type='polish'`, не `'tweak'`** — §3 (таблица
  ролей) говорит «создаёт tweak-задачи», а §9 говорит «tweak/polish». Схема
  журнала (§4) явно перечисляет `polish` как отдельный тип — выбрал его,
  т.к. он точнее отличает «сам себя нашёл на обживании» от «клиент попросил
  tweak»; оба в итоге пилятся тем же координатором (role=coder), разница
  чисто в трассировке/отчётности.
- **Один цикл live_in на сдачу — ограничение накладывает вызывающий
  (talk.js), не сам agents/livein.js.** `runLivein()` — чистая функция без
  состояния «уже вызывался»; `talk.js:runProjectFlow` вызывает её РОВНО
  один раз, после того как ВСЕ задачи дерева (включая регрессию) дошли до
  done, и не зацикливается на повторное обживание после polish-фикса —
  соответствует «лимит: один цикл на сдачу» буквально (§9).
- **Отчёт консультанта — новый файл `prompts/advisor.report.md`**, не
  переиспользование `advisor.analyst.md`. DEV_GUIDE §0 не перечисляет этот
  файл явно в дереве репозитория, но §17/§8(Фаза 5.5 DoD) описывают
  СТРУКТУРНО другой протокол той же роли advisor_analyst (не Q&A, а
  сводный отчёт с обязательными разделами) — по аналогии с тем, как
  advisor_intake уже имеет 3 отдельных файла протоколов (wish/problem/spec)
  под одной ролью, добавил четвёртый протокол-файл для той же роли-аналитика.
  Различение вызовов внутри одного mock-хендлера — по `opts.agent`
  (`'consultant_report'` vs `'advisor_analyst'`), а не по `promptFile`,
  т.к. MOCK диспетчеризует строго по role, не по файлу.
- **mock.js: viewport-поправка (polish-задача) требует явного приоритета
  над `isPage`** — реальный баг найден при первом прогоне: заголовок
  polish-задачи («index.html: добавить meta viewport...») тоже содержит
  подстроку «index.html», из-за чего мок-кодер ошибочно матчился в ветку
  isPage (переписывал файл БЕЗ meta viewport) вместо ветки viewport-фикса.
  Исправлено явным приоритетом (`isPage = /index\.html/.test(title) &&
  !isViewportPolish`) — тот же класс проблемы, что и заголовок tweak-задачи
  vs критерий в Фазе 3 (сопоставление по подстроке в тексте, где встречаются
  чужие подстроки).

## Известное отклонение от инварианта §20.10

§20.10: «MOCK-ветка каждой фичи — ПОСЛЕ проверки реального пути, не до».
Для Фазы 1 (gateway.js) это соблюдено буквально — реальные вызовы
`claude -p` в терминале сделаны ДО написания MOCK-ветки шлюза (см. Фаза 0
запись про два маленьких реальных вызова). Для router/advisor/architect/
skill/live_in (Фазы 3 и 5.5) порядок ОБРАЩЁН: по прямому указанию в
задании («Реальные прогоны на живых моделях НЕ запускай») MOCK-ветки этих
ролей написаны БЕЗ предварительной проверки реального пути — реальная
проверка отложена в RUNBOOK.md целиком. Это осознанное отступление от
инварианта, продиктованное явной инструкцией пользователя в ЭТОЙ задаче
(она стоит выше базового порядка ТЗ), а не пересмотр самого инварианта —
после того как вы прогоните RUNBOOK.md §2-6, стоит перечитать код этих
ролей на предмет несовпадений реального ответа модели с тем, что
захардкожено в mock.js (структура промпта/ответа могла не совпасть с
реальным поведением модели ровно потому, что реальный путь не был
проверен первым).

## Баги с первого живого прогона пользователя (2026-07-14)

Ровно то, что предсказывало отклонение выше: первый реальный `npm run talk`
(желание — одностраничник по продаже машин из Кореи) вскрыл три реальных
дефекта. Все три починены в коде, не только описаны:

1. **MOCK и реальный режим делили один journal.db/skills.db/workspace.**
   `npm run mock` (который я же посоветовал прогнать первым) оставлял свои
   тестовые задачи и файлы в тех же файлах, что потом читал `npm run talk`
   — пользователь увидел мой тестовый `ТЕСТ_ПУТЬ_НАРУШЕНИЕ` вперемешку со
   своим реальным проектом. Фикс: `config.js` — пути `journal.db`,
   `skills.db`, `workspace/` теперь зависят от `MOCK` (`journal.mock.db`,
   `skills.mock.db`, `workspace.mock/` под MOCK=1). Заодно нашёл тот же
   класс бага в `evals/race.js` — он стирал РЕАЛЬНЫЙ `journal.db` при
   каждом запуске (`resetDb()` без изоляции); теперь `npm run race`
   тоже грузит `.env.mock` и работает только с изолированной БД.
2. **`architect.js` не проверял, удался ли `npm install`.** Пакет
   `playwright` (архитектор сам его выбрал для браузерной проверки по
   правилу §8.1) либо не встал, либо не успел встать за отведённое время —
   `npmInstall()` при этом либо падал тихо, либо просто не имел таймаута
   (мог зависнуть на постинсталле, скачивающем браузерный бинарник).
   Задачи создавались в любом случае, все 4 попытки первой задачи
   гарантированно упирались в `Cannot find module 'playwright'`, и вся
   зависимая цепочка (карточка/поиск/фильтры/макет) навсегда осталась в
   pending — не потому что «не сделал», а потому что зависела от
   задачи, которая технически не могла пройти. Фикс: `npmInstall()`
   получил таймаут (10 мин, с запасом на браузерный бинарник) и
   `maxBuffer`; `runArchitect()` теперь ЧЕСТНО останавливается с
   `ok:false`, если install не удался, — задачи вообще не создаются,
   вместо доведения до гарантированного тупика на 4 попытках.
3. **advisor_analyst (question-маршрут) получал только имена файлов, не
   содержимое.** На вопрос «что делает X» этого хватало, на «почему не
   работает Y» — нет, аналитик честно отвечал «не вижу код». Фикс:
   `runAnalyst()` теперь получает `workspaceDir` и строит те же «глаза»,
   что и кодер (`buildEyes()`), просто read-only — видит реальное
   содержимое файлов, не только список имён.

4. **Советник переспрашивал уже решённое, и root_spec стирался при каждом
   новом wish.** На втором заходе («добавь фильтр и поиск») советник
   спросил, где расположить фильтр (чистая вёрстка — должен был решить
   сам) и какие поля показывать на карточке (уже было решено в первом
   заходе, но советник об этом не знал физически — `startIntake` не
   получал ни root_spec, ни прежние `engineering_defaults`). Хуже: даже
   если бы получал — `setRootSpec()` в `talk.js` ПЕРЕЗАПИСЫВАЛ root_spec
   на каждом project-заходе, так что второй wish уничтожил бы решения
   первого ещё до того, как советник успел бы их прочитать. Это прямое
   нарушение назначения root_spec из §4/§15 («лекарство от goal drift» —
   ОН ОБЯЗАН быть стабильным).
   Фикс — три части: (а) `agents/advisor.js` — `startIntake`/
   `continueIntake`/`reviseDecisions` теперь принимают `rootSpec`/
   `engineeringDefaults` и кладут их в промпт отдельной секцией
   «Контекст УЖЕ существующего продукта»; (б) `prompts/advisor.*.md` —
   добавлено железное правило «решай сам, если есть общепринятый
   вариант» (расположение фильтра — НЕ вопрос клиенту, это дизайн, а не
   содержание) и явное указание наследовать существующий контекст;
   (в) `talk.js:mergeRootSpec()` — root_spec теперь ДОПОЛНЯЕТСЯ
   (объединение engineering_defaults, summary растёт припиской), а не
   перезаписывается; архитектор/live_in/отчёт консультанта получают
   объединённую картину, не только новый инкремент.
   Отдельного MOCK-регресса на «не переспрашивает существующее» не
   добавлял (мок-советник в сценарии всегда отвечает ready сразу же,
   ветка `hasExisting` синтаксически проверена, но не прогнана
   сквозным тестом) — стоит добавить в следующем цикле правок.

## Часть 2

### П§2 (миграции журнала)

- **`ensureColumn(d, table, column, ddl)` — PRAGMA table_info вместо
  try/catch на ALTER TABLE** — SQLite не даёт `ADD COLUMN IF NOT EXISTS`;
  try/catch проглотил бы и настоящие ошибки (опечатка в DDL, блокировка
  файла), не только «колонка уже есть». Явная проверка списка колонок —
  тот же паттерн, что уже применён к таблицам (`CREATE TABLE IF NOT
  EXISTS`), просто для ALTER TABLE эквивалента нет. `journal.js:migrate()`.
- **DoD «зелёный на обоих драйверах» проверен только фактическим
  better-sqlite3 на этой машине** — переключателя «форсировать
  node:sqlite» в кодовой базе нет (`db.js:pickDriver()` — try/catch на
  require, а не флаг), добавлять его ради одной проверки миграции — за
  рамками П§2. Вся новая DDL (CREATE TABLE IF NOT EXISTS, ALTER TABLE ADD
  COLUMN, PRAGMA table_info) — стандартные SQLite-операции без
  драйверно-специфичного синтаксиса, тем же классом, что уже проверенные
  на обоих драйверах таблицы Фазы 0 (DECISIONS Фаза 0: «оба драйвера
  проверены evals/race.js»). Реальная проверка node:sqlite конкретно для
  tools/task_deps — RUNBOOK, если/когда better-sqlite3 не соберётся на
  какой-то машине естественным образом.
- **Идемпотентность и отсутствие потери данных проверены на КОПИИ реальной
  `journal.db` этой сессии** (23 задачи, 162+ событий на момент проверки):
  скопировал файл, применил новую `migrate()`-логику дважды подряд (второй
  прогон — доказательство идемпотентности), проверил количество строк до/
  после и что реальный `journal.js` (не тестовый дубль логики) корректно
  мигрирует боевой `journal.db` при обычной загрузке модуля. Данные не
  потеряны, новые таблицы/колонка появились, оба запуска миграции прошли
  без исключений.
- **Настоящий fan-in регрессионной задачи (`deps: createdIds` вместо
  `blocked_by_task_id: createdIds[last]`) сделан здесь, в П§2, не отложен
  до Фазы 6** — П§2 прямо называет это закрытием упрощения из DECISIONS
  Фазы 3 (пункт 3 списка миграций); откладывать было бы новым дублирующим
  долгом поверх уже задокументированного. Проверено не только через
  `npm run mock` (зелёный), но и прямым SQL-запросом к `journal.mock.db`
  после прогона: `task_deps` действительно содержит по одной строке на
  каждую задачу дерева для соответствующей регрессии.
- **`claimNext`: одиночная колонка И task_deps — обе связки должны быть
  удовлетворены (AND), не только та, что заполнена** — П§2 формулирует это
  явно («требует, чтобы И колонка, И все строки task_deps были в
  done|merged»). На практике задача сейчас использует либо одно, либо
  другое (никто не заполняет оба одновременно), так что разницы в
  поведении для существующих задач нет — просто оставляю поведение
  буквально по спецификации, а не «ИЛИ» (что было бы слабее и молча
  разрешило бы гонку, если бы что-то когда-то заполнило оба поля).

### П§3 (Фаза 5 — GitHub)

- **`gh repo create <name>` без явного `<owner>/`** — П§3 текстом приводит
  `<owner>/<name>` как пример, но резолюция owner отдельным вызовом
  (`gh api user` или похожим) — лишний сетевой запрос ради косметики: `gh
  repo create` без owner уже создаёт репозиторий под текущим
  аутентифицированным аккаунтом. Упрощение осознанное, ничего не теряет.
- **dry-run (MOCK) не вызывает `ghAvailable()` вообще, даже для проверки
  «доступен ли gh»** — `gh auth status` сам по себе сетевой вызов
  (проверяет токен у GitHub). Задание прямо запрещает «do NOT invoke...
  do not hit... GitHub APIs» в MOCK/non-RUN_REAL режиме — поэтому dry-run
  ветка в `core/github.js:ensureProductRepo` возвращает строку команды до
  первого обращения к бинарю `gh`; единственная реальная операция в
  dry-run — `git remote get-url` (локальная, без сети), нужна, чтобы
  dry-run команда честно отражала, какая ветка (create vs push) выполнится.
- **MOCK-проверка живёт в `core/mock.js:runProjectPipelineScenario`, не в
  `bin/talk.js`** — `npm run mock` запускает `core/mock.js` напрямую,
  никогда не через `bin/talk.js` (REPL, не библиотека для импорта в
  скрипт); чтобы «прогресс-строки/новые ветки видны на mock-прогоне»
  (существующий инвариант этого репо для router/advisor/architect) не
  нарушился и для github.js, dry-run-вызов и его проверка (`gh repo create`
  в строке + имя репозитория) продублированы в mock-сценарии — вызов из
  `bin/talk.js:publishToGithub()` идентичен по контракту, отдельно не
  протестирован скриптом (только вручную, RUNBOOK Фаза 5).
- **`.env.example`: заменил плейсхолдер `# GH_TOKEN=`, оставшийся от
  Фазы 1 части 1, на `GITHUB_PUSH=1`** — П§3 прямо говорит «в .env ничего
  добавлять не нужно» для токена (у `gh` своя связка), старый плейсхолдер
  предполагал обратное и не соответствовал бы реальному поведению кода.

### П§4 (Фаза 5.7 — Кузница инструментов, §26)

- **Реальный баг, найден MOCK-сценарием при первом прогоне: регрессия
  игнорировала cwd-контракт tool-задач.** `type=tool` пишет и проверяется
  относительно `tools/<name>/`, но регрессия исполняет ВЕСЬ `regression_of`
  список под ОДНИМ `cwd=workspace` продукта (checkRunner не умеет разный
  cwd для разных под-критериев одного `regression_of` — контракт §8.9
  писался до Кузницы, когда все задачи дерева жили в одном workspace).
  Повтор критерия сборки инструмента относительно workspace ломался
  технически («Cannot find module run.js»), а не содержательно — ложный
  провал регрессии на зелёном дереве. Фикс: `agents/architect.js` —
  `type=tool` ИСКЛЮЧЁН из `regression_of` (сборка уже верифицирована один
  раз при регистрации, §26.2), но остаётся в `deps` (task_deps, П§2) —
  регрессия по-прежнему СТАРТУЕТ только после того, как инструмент собран,
  просто не пересобирает его заново. Для продукта важно, что он
  ПО-ПРЕЖНЕМУ КОРРЕКТНО ИСПОЛЬЗУЕТ инструмент (tool_run + потребитель
  результата остаются в regression_of), не что инструмент всё ещё
  собирается с нуля.
- **`core/tools.js` открывает СОБСТВЕННОЕ соединение (`openDb` на тот же
  `JOURNAL_DB_PATH`), не переиспользует внутренний `db()` из `journal.js`**
  — §26.1 требует таблицу `tools` именно В journal.db (не отдельной БД,
  «регистрация транзакционно связана с задачами»), но НЕ требует, чтобы
  CRUD жил в самом `journal.js` — П§4 явно называет `core/tools.js`
  владельцем `registerTool/getTool/listTools/findTools/runTool`. WAL уже
  включён (`db.js:openDb`, Фаза 0) — несколько соединений к одному
  WAL-файлу штатны для несвязанных операций (регистрация инструмента и
  обновление статуса задачи не обязаны быть одной ACID-транзакцией, только
  eventually consistent, чего WAL и так гарантирует).
- **Реестр в контекст кодера инжектится КОДОМ (`coordinator.js`, в клон
  `task.spec` перед вызовом `runCoder`), не через отдельный параметр
  `runCoder()`** — сигнатуру `runCoder({task, workspaceDir, runId})` менять
  не пришлось (динамика уже и так идёт через `task.spec`/`task.feedback`,
  §6.3); `agents/coder.js` остался НЕТРОНУТ. Тот же приём, что уже
  применён для tool-корня записи (`workspaceDir` — существующий параметр,
  просто другое значение) — реестр не новый механизм, просто добавка к
  уже проходящему через кодера тексту.
- **`ensureToolsGit()` — git init внутри `tools/` при КАЖДОМ входе в
  tool-задачу (как `ensureWorkspaceGit` для продукта), не строго «при
  первой регистрации»**, как буквально написано в §26.4. Дословное чтение
  создало бы окно, где неудачные попытки сборки (до первой успешной
  регистрации) вообще не коммитятся — тот же класс потери работы, ради
  которого существует auto-commit safety net (шрам 4). Коммит на КАЖДУЮ
  попытку (`loom-tool: coder attempt N on <task.id> (<name>)`) +
  ОТДЕЛЬНЫЙ коммит на успешную регистрацию (`loom-tool: <name> v<version>
  (<task.id>)`, дословно по §26.4) — оба существуют, просто git-репозиторий
  `tools/` появляется раньше, чем «при первой регистрации».
- **`tools/package.json` без поля `"type"`** — П§4 явно не упоминает эту
  правку, но `run.js`-скрипты по умолчанию пишутся в CommonJS-стиле
  (`require`/`module.exports`, тот же стиль, что и весь остальной продукт
  в этом репо) — без нейтрального `package.json` внутри `tools/` Node
  унаследовал бы `"type":"module"` от корневого `package.json` LOOM (ТОЧНО
  тот же баг, что уже найден и исправлен для `workspace/` в Фазе 1
  DECISIONS). Применил превентивно, не дожидаясь повторного реального
  инцидента — баг уже задокументирован, тратить реальный прогон на его
  переоткрытие не нужно.
- **`tool.json` обязателен ДАЖЕ если критерий прошёл** — `coordinator.js`
  читает `tools/<name>/tool.json` с диска ПОСЛЕ зелёного тестера и
  отказывается регистрировать (уводит попытку в retry с говорящим FAIL),
  если файла нет или он не парсится. §26.1 называет `tool.json`
  обязательным файлом контракта, но НЕ говорит явно, что делать, если
  архитектор написал критерий, не проверяющий его наличие (брак архитектора
  по правилу 10 Свода — «контракт задачи целиком», но доверять этому
  вслепую противоречит духу «доверие построено на недоверии», прямо
  процитированному в §26.0). Задняя защита харнесса, не полагается только
  на промпт.
- **События `tool_run`/`tool_registered`/`tool_network` — литеральные
  значения колонки `events.type`**, не вложены в `payload.event` под общим
  `type='status'`/`'action'` (как уже сделано для `claimed`/
  `path_lock_blocked`/`max_attempts_reached` в `coordinator.js` с Фазы 3).
  ТЗ v4 §4 перечисляет `thinking|action|test_result|status|usage` как
  ПРИМЕР, не CHECK-constraint (в схеме её нет — проверено). DoD П§4 прямо
  требует «события tool_registered/tool_run в events», читаемое как
  `SELECT * FROM events WHERE type IN (...)` без парсинга JSON — выбрал
  буквальное прочтение DoD.
- **Архитектор создаёт И `type:'tool'`, И `type:'tool_run'` задачи**, хотя
  §26.2 п.1 буквально говорит только про `type:'tool'` для архитектора
  (`tool_run` официально — путь researcher'а, §26.2 п.2). MOCK-сценарий
  26.6 прямо требует «архитектор выдаёт DAG с задачей type:'tool' + задачей
  type:'tool_run' + обычной задачей» — без этого инструмент, собранный
  архитектором внутри одного дерева, некому было бы вызвать в том же
  дереве. `prompts/architect.md` объясняет разницу и требует
  `depends_on_title`-цепочку от tool → tool_run → потребитель.
- **MOCK-фикстура demo.txt создаётся сценарием напрямую (`fs.writeFileSync`
  в `runForgeScenario`), не отдельной задачей дерева** — избегает
  необходимости в множественной зависимости (tool_run зависел бы И от
  сборки инструмента, И от задачи, создающей demo.txt — task_deps это уже
  умеет с П§2, но городить это только ради демонстрации Кузницы —
  избыточно). Реальный сетевой/многозвенный кейс — предмет Фазы 7
  (researcher), не этой фазы.
- **Docker-песочница для `runTool()` НЕ реализована в этой фазе** — §26.3
  п.4 сама формулирует это как честное ограничение («локальный путь сеть
  физически не enforce'ит»), тот же паттерн, что уже принят для
  `checkRunner` в DECISIONS Фазы 4 (Docker — опциональный слой, демон
  недоступен в среде сборки). `tool_network`-событие логируется как факт
  аудита уже сейчас; сам Docker-путь для tools — по потребности, когда
  демон появится в среде (тот же RUNBOOK-пункт, что и для checkRunner).

### П§5 (Фаза 6 — параллельность, §16)

- **Реальный баг №1, найден MOCK-сценарием: `git merge --no-ff` в ветку БЕЗ
  единого коммита падает** («Non-fast-forward commit does not make sense
  into an empty head») — не конфликт, техническая ошибка. Однопоточный путь
  этого никогда не задевал (там нет `merge --no-ff`, только `autoCommit`);
  параллельная фаза создаёт worktrees и мёржит В main ДО того, как кодер
  вообще успел что-то закоммитить. Фикс: `coordinator.js:ensureInitialCommit()`
  — явный стартовый коммит перед первым `ensureWorktree()`, вызывается
  только из параллельной фазы (однопоточный путь не тронут).
- **Реальный баг №2, найден ТЕМ ЖЕ MOCK-прогоном сразу после фикса №1:
  `ensureInitialCommit` с `--allow-empty` БЕЗ `git add` оставлял
  `package.json` (уже написанный `ensureWorkspacePackageJson` НА ДИСК main
  до этого коммита, Фаза 1) неотслеженным.** Worktrees ветвились от
  коммита БЕЗ package.json в истории; каждый воркер, работая в своём
  worktree, находил, что package.json «отсутствует», и создавал СВОЙ
  (та же `ensureWorkspacePackageJson`, вызванная уже для worktree-пути) —
  при мёрже git отказывался переписать untracked-файл main'а версией из
  ветки («would be overwritten by merge», данные могли бы потеряться).
  Фикс: `git add -A` перед стартовым коммитом — всё, что уже лежит на
  диске main на момент первого входа в параллельную фазу, становится
  частью истории, worktrees наследуют его сразу. Оба бага — ровно то, для
  чего нужен MOCK ПЕРЕД реальным прогоном (§24): нашлись за секунды,
  бесплатно, до единого реального вызова модели.
- **`claimed_by` теперь НЕ обнуляется при переходе в `merge_pending`**
  (только раньше обнулялся при ЛЮБОМ статусе, кроме `claimed`) —
  `core/merge.js:mergeOneTask` читает `task.claimed_by`, чтобы понять,
  КАКОЙ worktree/branch (`loom/w<i>`) сливать; без фикса это поле уже было
  бы `NULL` к моменту, когда `drainMergeQueue` добирается до задачи (баг
  найден при проектировании, до первого прогона — `journal.js:setStatus`).
- **regression/tool_run исключены из параллельного захвата** через новый
  параметр `claimNext(..., {excludeTypes})`, не через отдельный «peek и
  вернуть в pending» — чище (не тратит фиктивный захват) и симметрично
  `activeTouches` (тот же объект опций). Обе роли структурно нуждаются в
  `workspaceDir=main` (`tool_run` реально исполняет инструмент с этим cwd;
  regression проверяет ИНТЕГРИРОВАННОЕ состояние продукта) — в worktree
  воркера они увидели бы чужую, не залитую в main версию. §16 п.8 говорит
  это же про регрессию текстом («строго после пустой очереди мёржей»);
  `tool_run` там прямо не упомянут, но по конструкции (§26.3 п.1: cwd =
  workspace текущего продукта) требование то же самое.
- **Искусственный конфликт в MOCK-сценарии — через НЕДОдекларированный
  touches_files, не через одинаковый touches_files у обеих задач.** Если
  бы обе конфликтующие задачи честно объявили `shared.js` в touches_files,
  weave вообще не дал бы им попасть в один раунд — сценарий тестировал бы
  только вход в weave-фильтр, не реальный git-конфликт и reconciler. Это
  одновременно и работающий тест, и честная демонстрация ГРАНИЦЫ file-level
  weave (§16 п.3 прямо признаёт это ограничение — touches_functions/
  tree-sitter сознательно за рамками v1.0-final): конфликты, невидимые
  декларации, всё ещё возможны, и именно поэтому reconciler — не опция, а
  обязательный второй рубеж защиты.
- **tsc-ворота: `resolveTscBin()` ищет ТОЛЬКО локально установленный `tsc`
  (`node_modules/.bin/`), никогда не вызывает `npx tsc` напрямую** — `npx`
  без предустановленного пакета молча качает его из сети, что прямо
  запрещено в задании для MOCK/без RUN_REAL. `typescript` добавлен в
  `optionalDependencies` (тот же паттерн, что `better-sqlite3`/
  `playwright`) — не установлен в этой среде сборки, ворота корректно
  graceful-degrade (`{ok:true, skipped:true}`), не блокируют merge
  недоступностью инструмента. MOCK-проверка «красной» ветки ворот — через
  `evals/fixtures/fake-tsc-impl.js`, подставленный как `node_modules/.bin/tsc(.cmd)`
  в тестовую директорию: реально исполняется как дочерний процесс (не
  замокан вызов функции), просто не настоящий `tsc` — тот же принцип, что
  `runTool` в §26.6 («реальный локальный процесс без модели и сети —
  мокать нечестно и не нужно»), применённый туда, где САМ бинарь
  недоступен по независящей от логики причине.
- **`depends_on_titles` (массив) — новое поле архитектора, ДОПОЛНЯЕТ
  `depends_on_title` (одиночный), не заменяет** — П§5 требует «touches_files
  обязателен, deps-массив» без уточнения имени поля/формата. Плюрализация
  по аналогии с уже существующим полем (не ломает старые DAG без этого
  поля) — `agents/architect.js` мапит оба на `deps` (task_deps, П§2) при
  создании задачи; `claimNext` уже требует одновременного выполнения ОБЕИХ
  форм зависимостей (П§2), новое поле просто даёт архитектору способ
  ЗАПОЛНИТЬ вторую форму для НЕ-регрессионных задач (раньше это делал
  только harness для авто-регрессии).
- **`evals/race.js`: внутрипроцессная weave-гонка НЕ доказывает атомарность
  BEGIN IMMEDIATE заново** (это уже честно доказано кросс-процессным
  сценарием — реальные ОС-процессы, реальная конкуренция за файл БД) —
  она проверяет, что JS-фильтр по `activeTouches` корректен именно в
  паттерне вызова, каким его использует `coordinator.js:claimAndRunInWorktree`
  (несколько `claimNext` из одного `Promise.all`-раунда, каждый со свежим
  `activeTouches`). 2 файла давали только 2 захвата за раунд независимо от
  числа воркеров (не баг — честное свойство file-level weave); тест
  переписан на 4 файла = 4 воркерам, чтобы раунды типично утилизировали
  всех воркеров, и на цикл до исчерпания очереди вместо фиксированного
  числа раундов (предохранитель — не бесконечный, явный FAIL при поломке).

### П§6 (Фаза 7 — глубина входа: researcher, ingest, банк/judge)

- **Весь стейт researcher-цикла — на доске (task/events), не в памяти
  процесса** (§10, п.6.1): `need_tools`/`tool_runs` из звонка 1 материализуются
  как обычные задачи `type='tool'`/`type='tool_run'` ДО их исполнения, звонок
  2 читает `task.feedback` уже done-задач + read-only глаза продукта — тот же
  принцип, что и у остального харнесса (журнал — источник правды, §4),
  просто применённый к новой роли.
- **`FICTIONAL_TOOL_CRITERIA`/`FICTIONAL_DATA_CRITERIA` (agents/researcher.js)
  — честный FAIL-заглушка, если researcher не прислал `criteria`/
  `data_criteria`.** Не выдумываем правдоподобный критерий за модель (это
  было бы симулированной проверкой, правило 5 §8) — задача просто гарантированно
  не сможет считаться выполненной, пока кто-то не поправит промпт/модель;
  предпочли явный, диагностируемый отказ тихому прохождению.
- **Стоп-фактор `data_unavailable` — структурно единственный путь при
  недоступности данных**: `bin/talk.js:runProjectFlow` / `bin/ingest.js`
  не содержат ветки «продолжить без данных» вообще — `return` до вызова
  архитектора. Так §10 инвариант выражен кодом, а не только промптом.
- **MOCK: маркер OK/NODATA для researcher-оценки прокидывается через
  `tool_runs[0].args` (видны в title задачи), не через текст брифа.**
  Причина: спека прямо говорит, что звонок 2 (оценка) видит ТОЛЬКО отчёты
  tool_run + глаза продукта, НЕ исходный текст problem — маркер в брифе был
  бы недоступен на этом звонке физически, а протаскивать его отдельным
  скрытым каналом мимо контракта — нечестный мок. Через `args`→`title` —
  честно, тем же путём, каким узнал бы и человек, читающий отчёт задачи.
- **`covers:[reqId]` — новая колонка `tasks.covers` (не отдельная таблица)**:
  тот же паттерн, что `tasks.tool_name` (П§2 `ensureColumn`, идемпотентно на
  существующей БД). Требование без единой покрывающей задачи ищется в JS
  (`agents/architect.js`: множество всех `covers` минус список id
  требований) — не блокирует прогон, только отчётность человеку
  (`uncoveredRequirements` в результате `runArchitect`), симметрично тому,
  как judge (`evals/judge.js`) — метрика, не ворота.
- **`bin/ingest.js` — самостоятельная точка входа, НЕ импортирует
  `bin/talk.js`.** `talk.js` вызывает `main()` безусловно на верхнем уровне
  модуля (запускает REPL) — импорт запустил бы чужой REPL как побочный
  эффект импорта. Общий хвост (architect→coordinator→live_in→отчёт→publish)
  поэтому вынесен внутри `talk.js` в `runBuildFlow()` (используется и
  `/ingest`, и обычным project-потоком), а `bin/ingest.js` — отдельный
  скрипт с тем же хвостом, продублированным по образцу уже существующего
  `bin/cli.js` (который тоже не переиспользует код `talk.js` из-за того же
  ограничения) — альтернатива (экспортировать `runBuildFlow` и обвязку из
  `talk.js`, обернув REPL-старт в `if (import.meta.url===...)`) была
  отклонена как более рискованная правка стабильно работающего файла ради
  вторичного CLI-входа.
- **MOCK-разбор документа (ingest) читает универсальный формат `- текст`
  построчно, а не подстраивается под конкретную фикстуру.** Мок-советник в
  режиме «разбор документа» распознаёт requirement-строки по markdown-списку
  (`^- `), что работает для ЛЮБОГО документа в этом формате, не только для
  `evals/fixtures/spec-package/`; это ближе к реальному поведению модели
  (которая тоже будет ориентироваться на структуру списка), чем матчинг по
  содержимому конкретных файлов.
- **Фикстура `evals/fixtures/spec-package/` содержит ДВЕ независимые дыры,
  проверяющие ДВА разных механизма:** `TODO_HOLE`-маркер в
  `02-payment.md` — требование без проверяемого критерия, ловится ingest'ом
  ДО архитектора (§11 п.3); мок-архитектор нарочно не покрывает ПОСЛЕДНЕЕ
  по счёту требование (`03-design.txt:R1`) — ловится трассировкой `covers`
  ПОСЛЕ архитектора (§11 п.4). Разные req_id намеренно — тест доказывает,
  что harness различает «дыра в самом ТЗ» и «архитектор забыл про
  требование», а не просто повторно детектирует одну и ту же дыру дважды.
- **`evals/bank.js`: под MOCK route НЕ проверяется, только структура
  интервью.** Мок-роутер — детерминированный регэксп (не настоящая
  диагностика), умеющий отличить problem от wish только по спец-маркеру
  (`ТЕСТ_ИССЛЕДОВАТЕЛЬ`, уже используется researcher-сценарием
  `core/mock.js`); банк намеренно использует ЕСТЕСТВЕННЫЙ текст желаний
  (не маркеры) — под MOCK протокол берётся из фикстуры напрямую
  (`protocol_expected`), `route_expected` сверяется только при `MOCK=false`
  (полный релизный прогон с живой моделью, см. RUNBOOK «выпускной экзамен»).
  Тот же принцип уже применяет `evals/interview.js`.
- **`evals/judge.js` логирует событие `eval_score` ДАЖЕ при сбое шлюза**
  (`result:null, error:"..."`) — отсутствие оценки тоже сигнал (модель
  недоступна/лимит), выбрасывать его молча означало бы терять данные для
  последующего анализа промптов (§14.3).
- **pdf/бинарные макеты в ingest — честная строка-заглушка
  (`agents/ingest.js:readDocuments`), реальный парсинг pdf — сознательно
  за рамками v1.0-final** (дословно оговорено в самом DEV_GUIDE part2 §6.2
  п.1 — фиксируется здесь для единообразия формата решений).

### П§7 (Фаза 8 — FTS5, дашборд, maintenance-минимум, Telegram)

- **Реальный баг, найден `evals/fts.js` на 106-й записи (не на глаз):
  `INSERT INTO fts_table(...) SELECT ... FROM src WHERE id NOT IN (SELECT
  rowid FROM fts_table)` молча портит инвертированный индекс FTS5, когда
  `fts_table` — external-content виртуальная таблица.** `COUNT(*)` после
  такого backfill честно показывал верное число строк (106), но `MATCH`
  ничего не находил — таблица одновременно и источник, и назначение внутри
  ОДНОГО statement, для виртуальных таблиц SQLite это недокументированное
  поведение. Фикс в `core/skills.js`/`core/tools.js`: `INSERT OR IGNORE
  INTO fts_table(...) SELECT ... FROM src` (конфликт по rowid молча
  игнорируется) — тот же результат, идемпотентность без самоссылки в
  подзапросе. Урок общий: любая внешне-контентная FTS5-таблица, которую
  бэкафиллят повторно (не только при первом создании), должна использовать
  `OR IGNORE`, не `NOT IN`.
- **Поддержка FTS5 сборкой sqlite пробуется ОДИН раз за процесс и
  кэшируется, но порог count≥100 пересчитывается на КАЖДЫЙ вызов
  search*/findTools.** DEV_GUIDE говорит только «при count≥100 создать
  таблицу», не уточняя частоту проверки; выбран вариант, ловящий переход
  порога В СЕРЕДИНЕ живого процесса (не только при следующем перезапуске) —
  `evals/fts.js` тестирует именно это (90 записей → LIKE, +15 в ТОМ ЖЕ
  процессе → FTS5), а не два отдельных запуска. `COUNT(*)` на таблицах
  такого размера (сотни-тысячи строк для одного проекта LOOM) дешевле, чем
  риск залипнуть на устаревшем пути до перезапуска.
- **`bin/dash.js` экспортирует `createDashServer()` (фабрика, не запускает
  `listen()` сама), автозапуск — только под `if
  (import.meta.url===pathToFileURL(process.argv[1]).href)`** — тот же
  паттерн, что уже применялся в `core/mock.js`. Без этого `npm run mock`
  не смог бы поднять сервер на эфемерном порту (`listen(0, ...)`) для
  MOCK-смоука без второго процесса — тест дёргает `/`, `/api/tasks`,
  `/api/task/:id` (включая 404 на несуществующий id), `/api/ask`
  (включая 400 на пустой question), затем гасит сервер и проверяет, что
  состояние доски не изменилось (дашборд — read-only интерфейс по
  конструкции, тест это утверждает явно, не предполагает).
- **`regression_detected` НЕ потребовал новых веток `coordinator.js`.**
  Прочитан код: только `type==='tool'|'tool_run'|'regression'` (точная
  строка) получают особые ветки; всё остальное (включая `project`/`tweak`/
  `polish`/`regression_detected`) уже идёт обычным путём кодера. «Принимается
  журналом и маршрутизируется как tweak» (§18, П§7 п.3) — уже истина by
  construction, задача решена подтверждающим MOCK-тестом
  (`core/mock.js:runMaintenanceScenario`), а не новым кодом. `bin/cli.js`
  уже пропускал произвольный `--type` без валидации — добавлена только
  строка в `usage()`, документирующая путь заведения такой задачи вручную
  (реального prod-мониторинга, порождающего эти задачи автоматически, в
  v1.0-final нет — честно за рамками, §18 сам это оговаривает: «полный
  контур PR→CI→staging→прод — сознательно ПОСЛЕ v1.0-final»).
- **`AUTO_MERGES_PER_DAY` — гейт в `drainMergeQueue`, считает `events`
  `type='merged'` за последние 24ч, останавливает очередь ДО обращения к
  задаче (не после)** — оставшиеся `merge_pending` не трогаются (не
  `failed`, не `blocked_needs_human`) и честно ждут следующего окна;
  следующий цикл координатора (в реальности — следующий день) сам их
  подберёт. Default = 1000 — гейт реально enforced (не просто константа
  для галочки), но не мешает обычной разработке/MOCK-параллельности (до
  нескольких мёржей за один прогон). `drainMergeQueue` принимает
  необязательный `maxPerDay`-override параметр специально для теста —
  не нужно ждать реальные сутки или гонять полный параллельный сценарий
  ради проверки гейта, только сам факт остановки очереди.
- **`bin/telegram.js` — заведомо УРЕЗАН относительно `bin/talk.js:
  runBuildFlow`: БЕЗ researcher/ingest/live_in-стадий.** §7 прямо разрешает
  «не собралось за день — в бэклог без сожалений» для этого модуля;
  решение — не притворяться полным перед пользователем: `finishProject()`
  проходит только advisor→architect→coordinator→отчёт, обживание явно
  помечено текстом в отчёте («только в npm run talk»). Состояние диалога
  interview (chat_id → protocol/history) держится В ПАМЯТИ процесса
  `bin/telegram.js`, не в journal — это временный контекст ДО брифа,
  симметрично тому, как `talk.js` держит `history` в переменной REPL-цикла,
  а не на доске. `parseUpdate()` — чистая функция (юнит без сети);
  `handleIncoming()` тестируется в MOCK с подставленным `globalThis.fetch`
  (мокается ТОЛЬКО сетевая точка выхода на `api.telegram.org`, вся
  остальная логика — реальная, через MOCK-модели, тот же принцип, что и
  Кузница/tsc-ворота: подменяется граница с внешним миром, не код).
- **`npm run mock` теперь ЕДИНЫМ прогоном покрывает П§8 DEV_GUIDE part2
  требование дословно**: калькулятор + Кузница (26.6) + параллельный
  8×2-конфликт + researcher (оба вердикта) + ingest-фикстура (обе дыры) +
  dashboard smoke + maintenance-минимум + Telegram-юнит — все сценарии в
  ОДНОМ вызове `core/mock.js`, раздельные MOCK-state директории (уже закон
  с бага №1 первого прогона) сохранены для каждой новой добавленной
  подсистемы.

### П§9 (долги части 1 — закрыты по пути)

Три пункта дословно из П§9 DEV_GUIDE part2:

1. **MOCK-регресс «советник не переспрашивает уже решённое» — закрыт.**
   `agents/advisor.js:buildUserText` экспортирована (была приватной) и
   прогнана напрямую в `core/mock.js:runContextInheritanceRegressionScenario`:
   без `existingContext` секция «Контекст УЖЕ существующего продукта»
   отсутствует; с ним — включает rootSpec И прежние `engineering_defaults`
   дословно. Заодно закрыт СОПУТСТВУЮЩИЙ долг, обнаруженный при написании
   этого теста: `mergeRootSpec` — та же 6-строчная функция накопления
   root_spec — была скопирована в `bin/talk.js`, затем ещё раз в
   `bin/ingest.js` (П§6.2) и `bin/telegram.js` (П§7) — три независимые
   копии одной и той же логики слияния состояния, риск разъехаться при
   правке одной и забытых остальных. Вынесена в `core/journal.js`
   (естественное место — рядом с `setRootSpec`/`getRootSpec`, которые она
   же и оборачивает), все три `bin/*` теперь импортируют одну функцию;
   MOCK-тест проверяет и её (второй заход дополняет `engineering_defaults`/
   `summary` первого, ничего не теряет).
2. **Перечитать `mock.js` на расхождения с реальным поведением моделей
   после первых реальных прогонов части 1 — ОСТАЁТСЯ ОТКРЫТЫМ.** Мастер-
   промпт этой сессии прямо запрещает реальные вызовы моделей без явного
   `RUN_REAL=1` от пользователя — реальных прогонов части 1 в рамках этой
   сессии не было, значит и расхождений фиксировать не из чего. Не
   закрывается искусственно (не выдумываю «перечитал и всё совпадает» без
   единого реального вызова) — остаётся пунктом `docs/RUNBOOK.md` после
   того, как человек пройдёт реальные разделы 2–7 части 1.
3. **`evals/manual/*` приведён к общему виду с `evals/bank`/
   `evals/fixtures/` — закрыт.** JSON-фикстуры задач (`sum-task.json`,
   `word-count-tool-task.json`, `word-count-tool-run-task.json`)
   переехали в `evals/fixtures/` (тот же принцип, что уже применялся к
   `evals/fixtures/spec-package/` и `evals/fixtures/fake-tsc-impl.js`:
   `evals/fixtures/` — входные данные, `evals/manual/` — только JS-драйверы
   реальных прогонов); `docs/RUNBOOK.md` обновлён на новые пути.
   `evals/manual/docker-isolation-task.js` и `evals/manual/parallel-real-run.js`
   остались на месте — это исполняемые скрипты, не фикстуры.

## Регрессия после сдачи части 2 (2026-07-16) — tsc-gate MOCK-тест зависел от окружения

Аудит по ТЗ v4 (сверка §1–§26 с кодом) начался с контрольного прогона
`npm run mock` — и он оказался КРАСНЫМ (§24 нарушен: «MOCK обязателен к
зелени... после любой правки машины»), хотя ни один файл кода не менялся.

**Причина:** на момент написания П§5 (DECISIONS) `typescript` был
optionalDependency и физически ОТСУТСТВОВАЛ в `node_modules` — юнит-тест
tsc-ворот в `core/mock.js:runParallelScenario` полагался на этот реальный
факт среды, чтобы проверить ветку graceful-degrade (`resolveTscBin()` не
находит бинарь → `{ok:true, skipped:true}`). Между тем сессией `npm install`
реально поставил все три optionalDependencies (`better-sqlite3`, `playwright`,
`typescript` — видно в незакоммиченных `package.json`/`package-lock.json`).
С `typescript` на месте `resolveTscBin()` находит РЕАЛЬНЫЙ
`ROOT/node_modules/.bin/tsc`: тестовый scratch-каталог (`ROOT/.tsc-gate-scratch/no-bin`)
случайно лежал на ТОЙ ЖЕ глубине от ROOT, что и настоящий worktree
(`ROOT/.worktrees/w1`), поэтому фолбэк-кандидат `worktreePath/../../node_modules/.bin`
из обоих путей резолвится в один и тот же реальный `ROOT/node_modules` —
тест непреднамеренно проверял не «typescript недоступен», а «typescript
недоступен ПРЯМО СЕЙЧАС на этой машине», что перестало быть правдой.

**Фикс:** `core/mock.js` — scratch-каталог tsc-gate теперь строится в
`os.tmpdir()`, а не внутри дерева ROOT, так что двухуровневый fallback-путь
`resolveTscBin()` физически не может попасть на реальный `ROOT/node_modules`
ни при каких обстоятельствах установки зависимостей. Сама
`core/merge.js:resolveTscBin()` не тронута — её расчёт глубины корректен
именно для настоящих worktree (`ROOT/.worktrees/w<i>`), баг был только в
тестовом дублировании той же глубины внутри ROOT. `npm run mock` снова
зелёный (`ИТОГ: все сценарии зелёные`, exit 0).

Урок: MOCK-тест, проверяющий «пакета нет», не должен полагаться на
случайное отсутствие пакета в реальном `node_modules` разработчика —
он обязан гарантировать это конструкцией scratch-окружения, иначе первый
же `npm install` без `--no-optional` красит доску без единой правки кода.

## Реальный инцидент (2026-07-16) — сломанный критерий заморозил дерево snake, scar 34

Пользователь сравнил Snake, собранный LOOM (`workspace/`), с тем же заданием,
выполненным Claude Code напрямую (`snake/`), и результат LOOM выглядел заметно
беднее. Разбор показал: дерево snake в `journal.db` было НЕ достроено —
задача «UI: стартовый экран, счёт и рекорд...» ушла в `blocked_needs_human`
после 4/4 попыток (потрачено $3.32 при бюджете $2), а обе зависимые задачи
(«Отрисовка и мобильная адаптивность canvas», финальная регрессия) вообще
не запустились. То, что лежало в `workspace/`, было застрявшим промежуточным
состоянием, а не финалом, который LOOM «выбрал» сдать.

**Причина:** архитектор (Opus) сгенерировал критерий с задвоенным
экранированием regex — в исходнике `.cjs`-файла оказалось `/\\d/` (два
реальных бэкслеша), которое ищет буквальную подстроку `\d`, а не цифру.
Прогон того же критерия напрямую через `checkRunner.runCheck()` против
РЕАЛЬНОГО кода UI-задачи (который на глаз выглядел полностью корректным)
детерминированно проваливался — что подтвердило: сломан был критерий, не
код. Кодер (Sonnet, эскалация на Opus на попытке 3) 4 раза честно чинил
рабочий код против невыполнимого теста.

**Разовый фикс (Task 1 отдельной сессии восстановления)** — исправлен
конкретный критерий в `journal.db` вручную (не новый API), задача
разморожена, дерево реально доведено до `done` на реальных моделях,
результат проверен визуально через Playwright-скриншоты (не только по
зелёному коду выхода — тот же урок, что и сам баг: не доверять чек-листу
слепо).

### Систематический механизм (Task 2, scar 34) — узнать «брак критерия», не только «брак кодера»

- **Сигнал (detectCriteriaDefect, `core/coordinator.js`):** критерий
  проваливается с ОДИНАКОВОЙ причиной (после нормализации чисел —
  `report.replace(/\d+/g,'#')`, т.к. координаты/счёт легитимно скачут между
  попытками при одной и той же причине) на КАЖДОЙ из MAX_ATTEMPTS попыток,
  И код кодера РЕАЛЬНО менялся между попытками. «Код менялся» проверяется
  git tree-хэшем коммитов автокоммита (`loom: coder attempt N on <taskId>`)
  — дёшево, честно, ничего не придумываем: если tree-хэш не различается,
  кодер буквально прислал то же самое, и это ОБЫЧНЫЙ повод для блока, не
  подозрение на критерий. Выбрана нормализация чисел, а не точное строковое
  сравнение — простая, объяснимая эвристика (правило §8.3 «минимумы, не
  точные числа» применимо и здесь), не fuzzy/ML-сравнение.
- **Кто чинит:** только `agents/architect.js:reviewCriterion()` — архитектор
  уже владеет критериями по §8 («нарушение любого правила = брак
  архитектора»), coordinator сам критерии не пишет и не имеет права их
  трактовать. Реализовано СИММЕТРИЧНО существующему `regenerateCriteria()`
  (тот же приём для противоположного брака — §8.8, критерий ВСЕГДА
  проходит): отдельный `userText` поверх ОБЩЕГО `architect.md`, роль/тир не
  меняются, отдельная mock-ветка различается по `opts.agent`, не по title.
- **Смещение осторожности:** при любой неопределённости, кривом ответе или
  сбое шлюза — `confirmed_block` (задача остаётся человеку). Ложное
  «исправление» рабочего критерия хуже, чем лишний блок — доверие построено
  на недоверии применимо и к самому этому механизму (иначе это был бы
  тихий люк, которым проваливающийся кодер выпрашивал бы себе более лёгкий
  тест).
- **Один review на задачу:** `detectCriteriaDefect` отказывается второй раз,
  если событие `criteria_defect_suspected` уже есть для этой задачи —
  никакой второй лестницы попыток поверх первой. Если исправленный критерий
  тоже не проходит через MAX_ATTEMPTS, дальше — обычный
  `blocked_needs_human` без повторного ревью.
- **`suspected_criteria_defect` — новый статус, не CHECK-constraint** (в
  схеме `tasks.status` его нет, TEXT свободный, как и для остальных
  статусов — проверено). Он транзитный: используется на время синхронного
  review-звонка внутри `handleAttemptFailure`, чтобы факт «идёт ревью» был
  виден в БД (диагностируемо SQL-запросом, §14), а не только в логах.
  Проверены все места, которые switch/`===` по `task.status` (`dash.js`,
  `mock.js`, `merge.js`, `talk.js`, `ingest.js`, `telegram.js`, `cli.js`,
  `evals/*`) — ни одно не падает на незнакомом значении, просто корректно
  не считает задачу готовой, пока статус не разрешится. `releaseStuck()`
  расширен: `suspected_criteria_defect` возвращается в `pending` наравне с
  `claimed` — тот же класс «процесс мог умереть посреди этого шага».
- **`journal.js:repairCriteria(id, criteria)` — новый узкий API**, не
  «отредактировать задачу как угодно»: меняет `criteria`, сбрасывает
  `attempts=0`/`feedback`/`question`/`claimed_by`, НЕ обнуляет `spent_usd`
  (деньги реально потрачены — история расхода не переписывается). Если
  задача уже исчерпала бюджет (обычный случай — MAX_ATTEMPTS почти всегда
  означает, что бюджет тоже на исходе или превышен, как было и в реальном
  инциденте: $3.32 при лимите $2), `budget_usd` продлевается на один
  стандартный дефолт (`DEFAULT_TASK_BUDGET_USD=2.0`, вынесен из `addTask`
  как общая константа) СВЕРХ уже потраченного — иначе `isOverBudget()`
  немедленно заблокирует задачу на первой же попытке ПОСЛЕ починки, так и
  не дав починенному критерию шанса. Это формализует ровно то, что было
  сделано вручную в Task 1 (там — одноразовый SQL, здесь — воспроизводимый
  механизм).
- **MOCK-покрытие (`core/mock.js:runCriteriaDefectScenario`)** — две
  задачи: «ЧИНИМ» (критерий буквально с тем же классом бага — задвоенный
  `/\\d/`, реальный код мок-кодера каждый раз содержит цифру, но всегда
  проваливается; после ревью пересобирается и проходит с первой попытки)
  и «ПОДТВЕРЖДАЕМ» (критерий корректен, мок-кодер каждый раз честно НЕ
  пишет требуемое — доказывает, что сама эвристика «одинаковый провал +
  разный код» НЕ ЯВЛЯЕТСЯ достаточным основанием чинить рабочий критерий,
  решает архитектор). Обе ветки проверяют конкретные события
  (`criteria_defect_suspected`/`criteria_repaired`/`criteria_defect_confirmed`)
  через SQL-подобный `listEvents`, не только финальный статус — тот же
  стандарт строгости, что и у остальных MOCK-сценариев этого репо.
  `npm run mock` и `npm run race` зелёные после добавления.

## Регрессия (2026-07-17) — live_in ронял прогон, когда playwright ставился, но браузеры нет (Г§13(1))

Реальный сбой на чистой машине: `npm install` тянет `playwright` как
`optionalDependencies` (пакет физически на месте), но браузерные бинарники —
отдельный шаг (`npx playwright install chromium`), который никто не
выполнял. `agents/livein.js:inspectWithPlaywright()` раньше ловила ошибку
фолбэком на статику ТОЛЬКО вокруг `import('playwright')` — если сам импорт
удавался, а `browserType.launch()` падал (ровно этот случай), catch-ветка
внутри возвращала `{ok:false, live:true, error}`. Это truthy-объект, а
`inspectProduct()` откатывался на `inspectStatic()` только по `live ||
inspectStatic(...)` — истинное значение НЕ откатывалось. Итог: `live_in`
возвращал `ok:false`, `runLivein()` пробрасывал ошибку, весь сценарий
`project pipeline` в `npm run mock` падал — нарушение Г§13(1) («MOCK
обязателен к зелени») из-за состояния окружения, а не правки кода.

**Фикс:** `inspectWithPlaywright()` теперь возвращает `{fallbackError:
e.message}` при падении ПОСЛЕ успешного импорта (launch/newPage/goto/
evaluate) вместо `{ok:false, live:true, error}`; `inspectProduct()` явно
проверяет `live?.ok`, а не полагается на truthiness, и передаёт
`live?.fallbackError` в `inspectStatic(entryFile, failureReason)` —
последняя честно называет причину («Playwright установлен, но живой рендер
не удался (...)») вместо общей «недоступен», и в обоих случаях (пакета нет
ИЛИ браузеров нет) note подсказывает конкретную команду `npx playwright
install chromium`.

**MOCK-покрытие без порчи окружения:** тот же урок, что и tsc-gate
регрессия выше — нельзя проверять ветку «launch падает», полагаясь на
случайное отсутствие браузеров именно на этой машине (здесь они реально
стоят и работают, см. `project pipeline` сценарий). Вместо удаления
браузеров/пакета `inspectWithPlaywright(entryFile, loadPlaywright)` и
`inspectProduct(workspaceDir, entryFileName, loadPlaywright)` получили
третий необязательный параметр — сеемую точку загрузки playwright
(по умолчанию `() => import('playwright')`). Новый
`core/mock.js:runLiveinLaunchFailureScenario` подсовывает загрузчик,
возвращающий объект с `chromium.launch`, который всегда бросает, и
проверяет: `evidence.ok===true`, `evidence.live===false`, `note` содержит
`playwright install chromium` — детерминированно, без зависимости от того,
установлены ли браузеры на машине, где реально гоняется `npm run mock`.
`npm run mock` и `npm run race` зелёные после добавления.

## §27 Stage A (2026-07-17) — designer role + design tokens (§27.2)

Первая стадия дизайн-контура (§27 ТЗ v4): доменный тег (§27.1) + роль
designer, издающая dizайн-систему ДО архитектора (§27.2). Решения в местах,
которые §27 оставляет на усмотрение реализации:

- **Домен решается один раз на проект и дальше "залипает" (sticky).**
  `bin/talk.js:runProjectFlow/runIngestFlow` вызывают `resolveDomain()`
  ТОЛЬКО если `getRootSpec('default').domain` ещё пуст; если проект уже
  когда-то решил домен — новое объявление советника/эвристика по свежему
  тексту игнорируется. Без этого повторный `project`-заход на тот же проект
  (например, клиент дозаказывает функциональность) мог бы перерешить домен
  по последнему инкременту текста и внезапно включить/выключить весь
  дизайн-контур посреди жизни продукта — §27.1 говорит "домен... наследуется
  всеми задачами проекта", что читается именно как "решается один раз".
  `journal.js:setRootSpec()` дополнительно страхует то же самое на уровне
  SQL (`COALESCE(excluded.domain, root_spec.domain)`) — вызывающие, которые
  вообще не знают о домене (`bin/telegram.js`, `evals/bank.js`, большинство
  MOCK-сценариев), не стирают уже принятое решение до NULL.
- **Эвристика-страховка (`core/domain.js:resolveDomain`) дефолтится в
  `"cli"`, не в первый домен из перечисления.** §27.1 описывает только
  случай "нашли UI-сигнал → ui"; для случая "советник домен не дал И
  сигналов нет" явного правила нет — `"cli"` выбран как безопасный дефолт
  ровно потому, что он (как и `"ui"`) гарантированно НЕ включает дизайн-
  контур, если продукт на самом деле не про интерфейс, а неверная
  классификация cli↔lib↔data друг с другом не имеет наблюдаемых
  последствий в v1 (домен управляет только веткой `=== 'ui'`).
- **Регэксп UI-сигналов — БЕЗ `\b`, но и без lookaround-конструкции
  advisor.js.** advisor.js уже задокументировал баг: `\b` в JS не считает
  кириллицу словом (`\w` — только ASCII), поэтому граница рядом с
  кириллицей не матчится. Здесь это тоже не используется, но по другой
  причине — сигналы (`html`, `canvas`, `страниц`, `экран`, `вёрстк`,
  `интерфейс`) достаточно длинные и самостоятельные, чтобы обычный
  substring-тест не давал ложных срабатываний, в отличие от коротких
  аббревиатур (`px`, `мс`), ради которых advisor.js понадобился lookaround.
- **`designer.js` не делает свой git-коммит — пишет tokens.css/DESIGN.md на
  диск и всё.** §27.2 говорит "харнесс... коммитит обычным autoCommit".
  Designer вызывается ДО архитектора, то есть ДО первой задачи в очереди —
  на этот момент `ensureWorkspaceGit`/`ensureInitialCommit` координатора,
  возможно, ещё ни разу не отработали (свежий проект), и репозиторий
  продукта может физически не существовать. Вместо дублирования
  git-инициализации внутри designer.js файлы просто ложатся на диск —
  `git add -A`, который уже происходит внутри `autoCommit()` на самом
  первом ходе кодера (и внутри `ensureInitialCommit()` для параллельной
  фазы), подхватывает их автоматически как часть первого же коммита. Это
  буквально "коммитит обычным autoCommit" — не отдельным, специальным
  коммитом.
- **Доставка кодеру — отдельный, всегда-полный блок в user-тексте
  (`agents/coder.js:buildDesignSystemBlock`), не через обычные "глаза".**
  `buildEyesForAttempt` на попытках 3+ показывает только дельту
  (added/changed/removed) относительно прошлого хода ЭТОЙ задачи — а
  tokens.css/DESIGN.md, однажды созданные designer'ом, между попытками
  кодера НЕ меняются и потому молча выпали бы из дельты именно тогда, когда
  кодер чаще всего должен их видеть (повторная попытка после FAIL по
  дизайн-критерию, §27.3). Блок гейтится по факту существования
  `design/tokens.css` на диске, не по domain — coder.js вообще не знает
  про domain.
- **Провал designer'а (невалидные токены после одной повторной попытки, или
  сбой шлюза) — честный останов ДО вызова архитектора, ни одной задачи не
  создаётся.** Симметрично уже существующему поведению архитектора
  (npm install пакетов не удался → останов до создания задач, см. Фазу 3
  выше): строить UI-дерево без изданной системы обесценило бы весь смысл
  роли designer, поэтому это не "мягкая деградация — продолжаем без
  дизайна", а жёсткий стоп с понятным сообщением человеку.
- **`agents/designer.js:validateTokensCss`** — та же техника, что §27.2
  прямо предлагает: скобочный баланс + регэксп деклараций, без
  CSS-парсера-зависимости. Проверяет: единственно `:root{}`/
  `[data-theme=...]{}` (никаких селекторов продукта), баланс `{`/`}`,
  минимумы деклараций по группам (`--color-*`≥5, `--space-*`≥4,
  `--radius-*`/`--font-*`/`--shadow-*`≥1). Комментарии CSS вырезаются перед
  проверкой, чтобы не давать ложных "посторонний селектор".
- **`bin/telegram.js` НЕ подключен к домен-гейту/designer'у в Stage A.**
  DECISIONS "Часть 2" уже фиксирует Telegram-вход как явно урезанный объём
  (без researcher/ingest/live_in) — тот же принцип применён здесь: это не
  забытая недоделка, а сознательное решение не расширять зафиксированный
  урезанный путь дополнительным механизмом того же порядка дороговизны.
- **Категория `design` в skills.db (§27.5) — НЕ реализована в Stage A**,
  по прямому указанию порядка сборки ("fold into whichever stage touches
  skill_writer, or a small fourth commit"). `agents/designer.js` пока не
  получает "дизайн-уроки" в контекст — это явный вырез объёма Stage A, не
  забытая недоделка, будет закрыто отдельным коммитом/стадией.
- **MOCK-покрытие 1/2/5 (§27.6)** — `core/mock.js`: `runDesignerScenario`
  (валидные токены → tokens.css/DESIGN.md на диске → реальный
  `runCoordinatorLoop`/`agents/coder.js` доставляет дизайн-блок кодеру,
  проверено подстрокой в его user-тексте через MOCK-ветку `ТЕСТ_ДИЗАЙН_
  ДОСТАВКА` → повторный вызов на том же проекте не переиздаёт и не тратит
  ни одного нового вызова модели), `runDesignerValidationScenario` (битые
  токены — селектор вместо `:root` — чинятся на повторной попытке ИЛИ
  остаются битыми навсегда и ничего не пишут на диск), `runDomainGateScenario`
  (явный домен побеждает эвристику, UI-сигнал ловится без объявленного
  домена, non-ui не тратит ни одного designer-вызова — буквально та же
  ветка `if (domain === 'ui')`, что в `bin/talk.js:runBuildFlow`, а не
  переимплементация). `npm run mock` и `npm run race` зелёные после
  добавления.

## §28 Stage A (2026-07-17) — spec_writer role + TZ contract (§28.2)

Первая стадия ТЗ-писателя (§28 ТЗ v4): `agents/specwriter.js` пишет полное
ТЗ документом ДО архитектора. Только модуль + валидация + запись файла —
ни `TZ_MODE`, ни вставка в `bin/talk.js`, ни утверждение человеком (это
Stage B), ни стыковка с ingest/covers (Stage C) в этом коммите не трогаются
(build order явно разносит их по стадиям, в отличие от §27, где "harness
heuristic in talk.js" уже входил в Stage A).

- **Заголовки секций — единственный канал проверки структуры.** §28.2
  говорит "harness проверяет детерминированно по заголовкам" — реализовано
  буквально: `REQUIRED_SECTIONS` — восемь фраз ДОСЛОВНО из промпта,
  `validateTzMd` ищет их внутри markdown-заголовков (`^#{1,4}...фраза`), не
  привязываясь к конкретному уровню/номеру заголовка (модель вольна писать
  `## 1. Цель и пользователи` или просто `# Цель и пользователи` — проверяется
  фраза, не форма). "За рамками" дополнительно проверяется на непустое тело
  секции (до следующего заголовка любого уровня) — та же техника, что уже
  использовал `agents/designer.js` для валидации токенов, тот же принцип
  "простая регэксп-проверка вместо парсера".
- **Требования проверяются ПОДСТРОКОЙ (`tzMd.includes(r.id)` /
  `.includes(r.text)`), не структурным парсингом markdown-списка.** §28.2:
  "harness сверяет id и тексты: расхождение markdown-документа с машинным
  массивом — брак". Точный substring-поиск — самый дешёвый способ
  гарантировать дословность без парсинга markdown-списков (модель может
  оформить список требований по-разному — жирным, нумерацией, дефисами —
  сверяется факт присутствия текста, не форма списка).
- **`runSpecWriter` без `workspaceDir` не пишет TZ.md и не считается
  ошибкой** — `tzPath: null` в ответе. Это не намеренная фича v1, а прямое
  следствие того, что Stage A тестирует модуль изолированно (MOCK-сценарий
  вызывает `runSpecWriter` напрямую, как и `runDesigner` в §27 Stage A);
  Stage B, где вызов идёт из `bin/talk.js` с реальным `WORKSPACE`, всегда
  будет передавать `workspaceDir`.
- **Лестница — одна повторная попытка на структурный брак, симметрично
  `agents/designer.js`.** В отличие от designer'а (где MOCK доказывает и
  "чинится на повторной попытке", И "остаётся битым навсегда"), §28.6 п.3
  явно описывает только один кейс для ТЗ — "снова без → blocked_needs_human"
  — поэтому Stage A проверяет только always-broken ветку (мок-`ТЕСТ_ТЗ_БЕЗ_
  РАМОК` держит секцию отсутствующей на обеих попытках намеренно); "чинится
  на повторной попытке" для ТЗ отдельно не тестируется, т.к. в спецификации
  не заявлен как отдельный сценарий.
- **`agents/ingest.js:findSpecHoles` уже реализует ровно то переиспользование,
  которое §28.4 требует для Stage C** ("режим «требования уже извлечены»" —
  `findSpecHoles({requirements, runId})` уже принимает готовый массив
  requirements, не документы) — рефактор `agents/ingest.js` для Stage C,
  скорее всего, не понадобится вообще, только новая точка входа, которая
  вызывает эту же функцию напрямую вместо `runIngest`.
- **MOCK-покрытие (§28.6 п.3 + запись файла):** `core/mock.js:
  runSpecWriterScenario` — сначала успешный путь (детерминированное ТЗ, 3
  требования, все 8 секций) в scratch-каталоге вне `workspace.mock`
  (изоляция, тот же принцип, что у `designer`/`live_in` scratch-сценариев),
  проверяет реальную запись `docs/TZ.md` на диск; затем структурный брак
  (`ТЕСТ_ТЗ_БЕЗ_РАМОК` — секция "За рамками" целиком опущена) — проверяется
  и текст причины провала, и ТОЧНОЕ число вызовов модели (`listEvents`
  `agent='spec_writer'` до/после — ровно 2, не больше и не меньше: доказывает
  "одна повторная попытка", а не ноль и не бесконечный цикл). `npm run mock`
  и `npm run race` зелёные после добавления.

## Реальный инцидент (2026-07-18) — `spawn npx ENOENT` на Windows, shim-команды вне gateway.js

Подтверждено на реальном железе (реальный прогон, не MOCK): задачи, которым
нужен `npx` (установка пакетов для Playwright-критериев, запуск инструментов
Кузницы), падали детерминированно с `Error: spawn npx ENOENT`. `where npx`
находил бинарь на PATH — причина не в окружении, а в том же классе бага, что
уже был найден и зафиксирован для `claude` в Фазе 1 (`gateway.js`): на
Windows `npm`/`npx`/`tsc`/любой JS-CLI ставится как `.cmd`-шим, а не
настоящий `.exe`, и `spawn`/`execFileSync` без `shell:true` (или без
`.cmd`-резолюции) не может его исполнить — задокументированное поведение
Node на Windows, не баг конкретного вызова.

**Аудит (`grep -rn "spawn(\|execFile\|exec("`  по `core/`, `agents/`,
`tools/`, `bin/`):** реальных недостающих фиксов в коде не нашлось —
`agents/architect.js:npmInstall` (установка пакетов) и
`core/merge.js:tscGate` (tsc-ворота) уже использовали `shell:isWin` +
ручное `JSON.stringify()`-квотирование, независимо повторяя паттерн
`gateway.js:buildSpawnArgs` вместо того, чтобы его переиспользовать — то
есть логика квотирования была задублирована вручную в трёх местах вместо
одного источника правды. Остальные spawn/execFileSync-вызовы (`git`, `gh`,
`docker`, `bash`, `node`) зовут настоящие `.exe`/бинарники, не `.cmd`-шимы —
этому классу бага не подвержены, трогать их не нужно.

**Фикс:** `core/spawnUtil.js` — новый общий модуль, `buildSpawnArgs(args)`
вынесен из `gateway.js` (поведение побайтово то же: `JSON.stringify` каждого
аргумента на win32, аргументы как есть на POSIX). `gateway.js` импортирует
хелпер вместо локального определения (рабочий Windows-фикс Фазы 1 не
тронут, только источник функции). `architect.js:npmInstall` и
`merge.js:tscGate` переведены на тот же импортированный хелпер вместо ручной
копии квотирования — один источник правды на все shim-вызовы.

**Инструмент-кузница (§26.3):** `core/tools.js:runTool` зовёт `node` напрямую
(настоящий бинарь) — не подвержен. Но сгенерированный код инструмента
(`tools/<name>/run.js`) — обычный Node stdlib-скрипт и МОЖЕТ сам вызвать
`child_process.spawn('npx', ...)` (например, инструмент, которому нужен
Playwright) без Windows-safe паттерна: у кодера не было соответствующей
инструкции в промпте (`prompts/architect.md` и `prompts/router.md` уже
требуют Windows-safe spawn для серверных критериев — тот же пробел для
Кузницы не был закрыт). Добавлена симметричная инструкция в
`prompts/coder.md` (§26 Кузница) с тем же паттерном
(`spawn(cmd, args, {shell: process.platform === 'win32', ...})` +
обязательный `.on('error', ...)`).

**Отдельно проверено и НЕ тронуто:** сообщение о провале `tool_run` для
`devkg-jobs-scraper` в той же сессии — это отсутствие записи `data/jobs.json`
инструментом, не `ENOENT` от spawn; два разных бага, смешивать фикс не
стал.

**Регрессия:** `evals/spawn-windows.js` (`npm run spawn-eval`) — мокает
`process.platform` (обе ветки, win32 и POSIX, прогоняются независимо от
реальной ОС хоста CI) и напрямую проверяет результат `buildSpawnArgs`:
кавычки на win32 обратимы (`JSON.parse` восстанавливает исходное значение,
включая путь с пробелом одним аргументом), аргументы на POSIX не меняются
вообще (кавычки без shell испортили бы значение). `npm run mock` и
`npm run race` зелёные после рефактора.

## §29 (2026-07-18) — «Изоляция проектов» (реальный инцидент, шрам 35)

Полный текст раздела — `docs/LOOM_TZ_v4_SECTION_29.md` (положен в docs/ и
закоммичен ДО начала реализации, по прямому указанию задачи). Три коммита,
build order из самой задачи: `phase 11a` (§29.1-2, ядро изоляции),
`phase 11b` (события + миграция legacy), `fix: remaining npx shim spawn
paths` (§29.5, отдельный хвост про npx, попавший в тот же тикет).

- **`releaseStuck({projectId})` — projectId НЕОБЯЗАТЕЛЕН, В ОТЛИЧИЕ от
  `claimNext`/`runCoordinatorLoop`.** Текст §29.2 п.1 явно требует "бросок
  исключения" только для `claimNext`; п.2 про `releaseStuck` такой
  формулировки не содержит. Разница — не невнимательность спецификации, а
  реальная необходимость: `bin/talk.js:main()` обязан звать
  `releaseStuck()` ДО того, как сеанс вообще выбрал активный проект (иначе
  курица-яйцо — нечем фильтровать на самом первом старте), а `bin/cli.js`,
  `bin/resume.js`, `evals/race.js`, `evals/bank.js` — служебные/ops-утилиты,
  которым в принципе незачем знать о конкретном проекте для банального
  "подобрать зависшие claimed после смерти процесса". Сделал `releaseStuck`
  оптационально скопированным: без `projectId` — честный глобальный sweep
  (ровно старое поведение), с `projectId` — сужение (новый путь,
  используемый изнутри `runCoordinatorLoop`, где активный проект уже
  известен и трогать чужой claimed чужого одновременно живущего процесса
  недопустимо).
- **`bin/resume.js` теперь проходит КАЖДЫЙ active-проект по отдельности**,
  не один глобальный проход. Раньше `/resume`-инцидент (§29.0) был именно
  про то, что один координатор-проход без фильтра исполнял чужие деревья;
  простое "добавить `--project` флаг" оставило бы старую дыру для человека,
  который забыл его передать. Вместо этого resume.js сам итерирует
  `listProjects({status:'active'})` и запускает `runCoordinatorLoop` для
  КАЖДОГО отдельно, своим `workspaceDir`/`projectId` — ни один проект не
  остаётся молча заброшен, но ни один проход не смешивает деревья.
- **`bin/telegram.js`: один чат = один проект, `project_id =
  'telegram-chat-<chatId>'`, создаётся лениво через `ensureProject`.**
  Telegram-вход и так документирован как урезанный (см. шапку файла,
  DECISIONS «Часть 2»); полноценный диалог "это новый продукт или
  текущий?", как в talk.js, для чата избыточен — весь смысл чата обычно
  один продолжающийся продукт. Не тестировалось на дизайн многопродуктовых
  Telegram-чатов (не входит в объём §29, дверь открыта в §29.6 духе).
- **Кузница (§26) осталась ГЛОБАЛЬНОЙ, не по проекту** — прямое указание
  §29.2 п.8: `tools`-реестр переживает проекты, это его смысл;
  `tool_run`-задачи по-прежнему принадлежат проекту как обычные задачи
  (`project_id` есть, `TOOLS_DIR` — нет). Решение, не пропущенный фильтр.
- **`core/projects.js` — отдельное соединение `openDb(JOURNAL_DB_PATH)`**,
  тот же паттерн, что `core/tools.js`/`core/skills.js` (несколько
  соединений к одному WAL-файлу штатны); таблица `projects` создаётся в
  `journal.js:migrate()` (схема — единый источник правды там же, где
  `tools`, хотя основной потребитель другой модуль — уже устоявшийся в
  проекте приём).
- **`events.project_id` НЕ прокинут явно через все logEvent-вызовы**
  (`gateway.js`/`tools.js`/`coordinator.js`/`merge.js` — десятки мест).
  Вместо этого `logEvent()` сама выводит `project_id` из `task_id` через
  `SELECT project_id FROM tasks WHERE id = ?`, когда вызывающий явно не
  передал `project_id`. Дешёвый lookup на каждое событие с task_id, зато ни
  один из существующих call site не тронут — колонка появляется "бесплатно"
  везде, где событие уже привязано к задаче. События без task_id (вызовы
  моделей до появления задачи — интервью советника) остаются с
  `project_id=NULL`, честно.
- **`migrateLegacyDefaultProjectId()` запускается на КАЖДОМ `db()`-старте**,
  не разовым скриптом, который человек может забыть выполнить. `UPDATE ...
  WHERE project_id='default'` — идемпотентно (пустой набор строк после
  первого прогона), `INSERT OR IGNORE` не дублирует запись `'legacy'`.
  Реальный `journal.db` автора мигрируется автоматически при следующем
  запуске ЛЮБОГО `bin/*.js` — не нужна отдельная миграционная команда.
  Старый `workspace/` (файлы трёх перемешанных продуктов) НЕ трогается и не
  расплетается — прямое указание §29.3 п.4, человеку решать руками через
  `/project legacy`.
- **Путь-замок (§12.1) не потребовал ни строчки правки** —
  `agents/coder.js:isPathInsideWorkspace(workspaceDir, relPath)` уже был
  параметризован базой; как только `workspaceDir` стал
  `<WORKSPACE>/<project_id>/` вместо глобального `WORKSPACE`, изоляция
  между проектами появилась бесплатно, тем же механизмом, что уже защищал
  от `../`. Единственная работа — MOCK-доказательство этого факта
  (`runProjectIsolationScenario`, §29.4 п.6), не новый код.
- **MOCK: `MOCK_PROJECT_ID = 'mock-scenario'` — единый на ВСЕ 19
  существующих сценариев**, механическая инъекция `project_id`/`projectId`
  первым полем в каждый `addTask`/`runCoordinatorLoop`-вызов (не по одному
  вручную — Node-скрипт с текстовой заменой по всему `core/mock.js`,
  проверено `node --check` + полный прогон). Это НЕ новый тихий дефолт —
  эквивалент старого неявного `'default'`, теперь явный и единственный на
  файл; два НОВЫХ сценария (`runProjectIsolationScenario`,
  `runLegacyMigrationScenario`) заводят СВОИ отдельные `project_id`
  (`createProject`, реальные записи с разными id). `bin/dash.js:/api/ask`
  внезапно потребовал резолвить проект через `core/projects.js`, поэтому
  `runDashboardSmokeScenario` пришлось передать `project: MOCK_PROJECT_ID`
  явно в теле запроса, а `main()` — завести реальный `projects`-ряд для
  `MOCK_PROJECT_ID` (`ensureProject`) до первого сценария: без него
  `getProject(MOCK_PROJECT_ID)` возвращал `null`.
- **§29.5 (остаточный npx ENOENT) — отдельный DECISIONS-абзац см. выше**
  (`## Реальный инцидент (2026-07-18) — spawn npx ENOENT на Windows...`) —
  структурный фикс на уровне `checkRunner.js`, не про изоляцию проектов, но
  прибит к тому же тикету/build order.

`npm run mock` (19 существующих + 2 новых сценария) и `npm run race`
зелёные после каждой из трёх стадий.
