# LOOM — Полное техническое задание
### Автономный инженер разработки. Единая спецификация v4 (объединяет опыт v1–v2, ТЗ v3 и целевую рамку проекта)

> Документ — единственный источник правды о том, ЧТО такое LOOM и КАК он устроен.
> Каждый механизм здесь существует не потому, что «так принято», а потому что его
> отсутствие однажды убило реальный прогон — см. Реестр шрамов (§21).

---

## 0. Что такое LOOM

**LOOM — автономный инженер, а не исполнитель промптов.** Devin, Lovable, Claude Code,
Codex строят то, что сказано, пока человек сидит рядом и направляет. LOOM занимает
другую позицию: берёт ответственность за результат — думает до, во время и после,
а человек возвращается к готовому продукту, а не к диалогу.

**Формула цели:** понять глубже, чем сказано → решить до дна, а не насыпать фич →
проверить дважды (код и впечатление) → отдать продукт, почти не требующий правок.

**Принцип глубины:** «больше, чем просили» = глубже в проблему, не шире в фичи.
Каждая инициатива системы проходит фильтр: *служит ли это исходной задаче клиента?*
Wow-эффект = чувство «меня поняли глубже, чем я сам сказал», а не количество кнопок.

**Анти-цели (что LOOM сознательно НЕ делает):**
- не допрашивает «зачем» того, кто пришёл с хотелкой;
- не выдумывает данные — никогда, ни при каких обстоятельствах;
- не делает вид, что проверил то, чего проверить нельзя;
- не строит «на всякий случай» — фича без связи с задачей не создаётся.

---

## 1. Пять типов входа = одна жизнь проекта

Вход один (сеанс/CLI/пакет файлов), типов запроса пять. Различает их Router (§3),
будя минимально необходимый набор ролей: *не собирай совещание там, где хватит
одного взгляда*.

| Тип | Пример | Интервью советника | Кто просыпается |
|---|---|---|---|
| **question** | «что делает app.js?», «почему бот не стреляет?» | нет | только аналитик, read-only |
| **tweak** | «поменяй цвет кнопки», «почини отступ» | нет | кодер + тестер |
| **wish** (хотелка) | «хочу VTOL-дрон, напиши кодовую часть» | только «что именно должно получиться» — НИКАКИХ «зачем» | полный цикл |
| **problem** (проблема) | «растёт купля-продажа машин, не знаю какие заказывать» | диагностика: проблема → критерий успеха клиента → право оспорить решение и предложить другое | полный цикл + Исследователь |
| **spec** (ТЗ-пакет) | папка с ТЗ, доками, макетами | разбор документов, поиск дыр и противоречий в самом ТЗ, вопросы только по дырам | полный цикл |

Это не пять режимов, а одна жизнь: сданный по spec продукт живёт дальше через tweak,
tweak рождает problem, problem рождает исследование и новые задачи — всё через одну
доску одного проекта. Эскалация встроена: tweak, оказавшийся сложнее (failed или
кодер вернул вопрос), автоматически перезапускается маршрутом project.

---

## 2. Архитектура: два уровня, которые не смешиваются

**Нижний — умный журнал** (SQLite): единственный источник правды. Гарантирует
атомарный захват (BEGIN IMMEDIATE), зависимости задач, трейсинг run_id, бюджеты.
Не думает — защищает целостность. Переживает любую смерть процесса: доска = память.

**Верхний — роли**: советник, router, архитектор, кодер, тестер, обживатель,
исследователь, секретарь уроков. Координатор — детерминированный код без LLM,
только процесс: захват → исполнение → вердикт.

**Принцип «учёт vs ум»:** механика (кто что взял, статусы, мёржи) — код;
сложные решения — мощная модель, вызываемая разово через шлюз.

---

## 3. Роли

| Роль | Файл | LLM | Модель (старт) | Пишет файлы |
|---|---|---|---|---|
| advisor_intake | agents/advisor.js | да | Haiku | нет |
| advisor_analyst | ask.js / talk «question» | да | Haiku | нет |
| router | agents/router.js | да | Haiku | нет |
| architect | agents/architect.js | да | **Opus** (осн.) → Sonnet | нет (только план) |
| coder | agents/coder.js | да | Sonnet → Opus (эскалация) | да, только в worktree/workspace |
| tester | agents/tester.js | **нет** | — | нет |
| live_in (обживатель) | agents/livein.js | да | Sonnet | нет (создаёт tweak-задачи) |
| researcher | agents/researcher.js | да | Sonnet → Opus | нет (заказывает задачи) |
| skill_writer | agents/skill.js | да | Haiku | нет |
| coordinator | coordinator.js | **нет** | — | нет |

Правила ролей:
- Координатор и Архитектор не редактируют файлы — делегируют. Код пишет только Coder.
- Tester и Coordinator — чистый детерминированный код.
- Конфигурация агента = JSON (model/endpoint/tools/maxTurns); движок и инструменты общие.

**Советник — инженер-приёмщик с протоколами под тип входа.** Общие законы:
клиент описывает результат — технические решения советник принимает сам и показывает
списком «Принял инженерные решения» (Enter = согласие, текст = корректировка);
технические вопросы клиенту запрещены **на двух уровнях** — промптом И regex-фильтром
кодом (px, мс, скорости, размеры, тайминги, протоколы, форматы); мусорный вход
(«continue», <15 символов) не превращается в бриф — вежливый отказ с подсказкой /resume.
Бриф всегда содержит `engineering_defaults` — архитектор использует их дословно.
Для problem-входа советник дополнительно фиксирует: формулировку проблемы, критерий
успеха клиента, принятое решение и почему именно оно (включая случаи «вы просили X,
но вашу проблему решает Y — делаю Y, вот обоснование»).

**Router** — один вызов лёгкой модели: `{route, reason, criteria?}` (criteria обязателен
для tweak, строится по правилам §8). Парсинг безопасный: снять фенсы, JSON.parse,
при ошибке → route=project (ошибка в сторону завышения безопасна). Дополнительно
`complexity_score` (files≤1 и <20 строк → trivial-путь).

**Обживатель (второй контур тестирования, §9)** — после зелёной доски садится
за продукт как первый пользователь: Playwright-проход глазами новичка по пути
клиента; каждая шероховатость → tweak-задача на доску с обоснованием. Лимит:
один цикл обживания на сдачу (стоп-фактор от вечной полировки).

**Исследователь (§10)** — включается для problem-входов и по запросу архитектора.

---

## 4. Журнал

### tasks
```sql
CREATE TABLE tasks (
  id TEXT PRIMARY KEY,
  title TEXT, spec TEXT,
  criteria TEXT,                -- машинно-проверяемый критерий (§8)
  role TEXT DEFAULT 'coder',    -- coder | architect | tester | researcher
  type TEXT DEFAULT 'project',  -- project | tweak | regression | research | polish
  status TEXT DEFAULT 'pending',
  touches_files TEXT, touches_functions TEXT,   -- JSON-массивы
  blocked_by_task_id TEXT REFERENCES tasks(id),
  budget_usd REAL DEFAULT 2.0, spent_usd REAL DEFAULT 0.0,
  claimed_by TEXT, attempts INTEGER DEFAULT 0,
  feedback TEXT,                -- ТОЛЬКО последний отчёт тестера, не история
  question TEXT, created_at INTEGER
);
```
Статусы: `pending → claimed → done | failed | blocked_needs_human | merge_pending → merged`.
`suspected_criteria_defect` (scar 34) — транзитный статус ПЕРЕД `blocked_needs_human`
на исчерпании MAX_ATTEMPTS: критерий проваливается с одинаковой причиной при
разном коде кодера — архитектор зовётся на ревью текста критерия ДО того, как
задача уйдёт человеку; чинит → `pending` заново, подтверждает → `blocked_needs_human`
как обычно. Тот же класс «процесс мог умереть посреди этого шага», что и
`claimed` — `releaseStuck()` возвращает и его в `pending`.
Захват: `UPDATE ... WHERE id=? AND status='pending'` внутри BEGIN IMMEDIATE;
`claimNext(role)` фильтрует по роли и разблокированным зависимостям. Убитый процесс
оставляет claimed-задачи — `releaseStuck()` возвращает их в pending при любом входе
(resume, talk, повторный запуск).

### events (LLM Ops, §14)
```sql
CREATE TABLE events (
  id INTEGER PRIMARY KEY, run_id TEXT, task_id TEXT,
  type TEXT,                    -- thinking | action | test_result | status | usage
  agent TEXT, duration_ms INTEGER, tokens_in INTEGER, tokens_out INTEGER,
  cost_usd REAL, payload TEXT, created_at INTEGER
);
```
run_id присваивается с ПЕРВОГО вызова модели. Диагностика = SQL, не чтение логов.

### skills.db — отдельная БД
BEGIN DEFERRED (слабая строгость: перезапись урока допустима). writeSkill()
вызывается механически координатором после каждой завершённой задачи.
Фильтр галлюцинаций: урок обязан ссылаться только на реальные файлы задачи;
упоминание чужих языков/файлов (.py/.cs/…) → отброс; нет урока → SKIP, ничего
не пишется. FTS5-поиск — только после 100+ записей.

### root_spec
Исходная цель проекта хранится в журнале ВНЕ контекста агентов и перечитывается
на каждой проверке (лекарство от goal drift, §15).

---

## 5. Шлюз моделей

Единственная точка звонков. Контракт: `chat(role, messages) → текст`;
`chat(role, messages, {json:true}) → уже распарсенный объект` — валидация ВНУТРИ
шлюза, кривой JSON = сбой модели (лог 200 символов сырого ответа, ретрай с
напоминанием, затем следующая модель цепочки), а не смерть прогона.

- **Стартовая конфигурация (текущее решение):** единственный провайдер —
  **claude-cli**; разные роли получают разные модели через флаг `--model`
  (суффикс в имени: `claude-cli:haiku|sonnet|opus`). Тиры: лёгкие роли — Haiku,
  кодер — Sonnet с эскалацией на Opus, **архитектор — в основном Opus** (качество
  дерева и критериев определяет весь прогон, тут экономить нельзя) с фолбэком
  на Sonnet при недоступности/лимите. Пример:

  ```javascript
  export const ROLES = {
    advisor:   ["claude-cli:haiku"],
    router:    ["claude-cli:haiku"],
    architect: ["claude-cli:opus", "claude-cli:sonnet"],
    coder:     ["claude-cli:sonnet", "claude-cli:opus"],   // вверх при провале grounding
    live_in:   ["claude-cli:sonnet"],
    researcher:["claude-cli:sonnet", "claude-cli:opus"],
    cheap:     ["claude-cli:haiku"],
  };
  ```
  Следствия зафиксированы: единая точка отказа (лимит подписки останавливает всю
  машину — доска ждёт /resume, работа сессиями между окнами лимитов); HTTP-ветка
  шлюза лежит в коде выключенной и включается конфигом без правки логики.
- **Провайдеры равноправны** архитектурно: OpenAI-совместимый HTTP (NIM/DeepSeek/любой) и
  `claude-cli` (Claude Code headless по подписке). Для claude-cli: системный промпт
  через `--system-prompt-file` (аргумент CLI на Windows режется по первому \n),
  пользовательский — через STDIN; изоляция вручную: `--tools ""`,
  `--strict-mcp-config` + пустой mcp.json, `--settings` с пустыми hooks
  (`--bare` не годится: требует API-ключ, не читает подписку).
- **Цепочки фолбэков** на роль + **два круга** с паузой 60с («все недоступны» в час
  пик — не смерть). **Две причины перехода** различаются: техническая (модель упала /
  таймаут) → следующая в цепочке того же тира; содержательная (ответила, но grounding
  провален) → эскалация на модель сильнее.
- `maxRetries: 0` в SDK — вся retry-логика только в шлюзе (дефолт SDK молча утраивает
  таймауты). Таймаут вызова 300с (180с убивал честно думающий DeepSeek).
- `MODEL_LIMITATIONS` в конфиге (пример: llama — один tool_call за ход → пропуск
  модели, если история многовызовная). THINKING per-role: reasoning выключен для
  advisor/router/cheap (минуты на «задай 3 вопроса» — расточительство).
- **Кэширование промптов:** системный промпт — статичный файл, побайтово идентичный
  между вызовами; вся динамика — в user message. Проверка: cache_read_input_tokens > 0
  на втором вызове. Лог usage (in/out/cache) каждого вызова → events.

---

## 6. Context Engineering

1. **Глаза кодера — дельта, не снимок.** Ход 2 — полный snapshot, ходы 3+ — только
   added/removed/changed. (v2 слал весь workspace: 34К токенов на ход.)
2. **Фильтр мусора в глазах:** файлы `.min.*`, файлы >50КБ, бинарники → строка-заглушка
   `"[библиотека chart.min.js, 180КБ — не читается]"`. Кодеру ЗАПРЕЩЕНО записывать
   сторонние библиотеки файлами — только CDN `<script src>` (инцидент: chart.min.js
   в глазах жёг ~15К токенов каждый вызов).
3. **Релевантность:** полные тексты — только файлам из spec/touches_files задачи,
   остальным — имя + первые 30 строк.
4. **Rolling summary** истории tool_use на уровне harness: держать последние N ходов,
   старое сворачивать в сводку.
5. **feedback = только последний отчёт** тестера, не накопление истории попыток.

---

## 7. Loop Engineering

> **Статус:** активируется при подключении tool_use-провайдера (HTTP) или переходе
> кодера на вариант Б (инструменты внутри headless). В стартовой claude-cli-конфигурации
> действует JSON-контракт кодера (вариант А): модель возвращает
> `{"files":{...}} | {"question":"..."}`, файлы пишет harness; пункты про
> submit_result/tool_choice — спящая спецификация, лестница попыток и auto-commit
> действуют с первого дня.

- Финал хода агента — инструмент `submit_result`: `tool_choice:"required"` + сужение
  списка до одного инструмента на последнем ходу (именной forced choice часть моделей
  молча игнорирует). Прозаический ответ вместо tool_call → retry-to-next-model на
  КАЖДОМ ходу.
- Лестница попыток задачи: 1) дешёвая модель → 2) та же, чистый контекст, root_spec
  заново → 3) модель сильнее того же тира → 4) blocked_needs_human. MAX_ATTEMPTS=4.
- **Auto-commit safety net:** кодер может забыть git_commit → «ложно смёржено»;
  коммит принудительно в конце хода кодера, если тот не закоммитил сам.
- Канал «вопрос человеку»: кодер вправе вернуть question → задача в
  blocked_needs_human, вопрос человеку в интерфейс, ответ приклеивается к spec.

---

## 8. Свод правил критериев приёмки (сердце доверия)

Критерии пишет Архитектор ДО кода; Кодер их видит, изменить не может (журнал,
куда его инструментам хода нет). Нарушение любого правила = брак архитектора.

1. Проверяется **поведение**, не исходник: регулярки по структуре кода запрещены.
   Сайты/игры — живой браузер (Playwright) через window-объекты и DOM;
   `pathToFileURL`, не склейка `file://`+путь.
2. Клиент дал свою проверку в брифе — используется **она**, дословно.
3. **Минимумы, не точные числа** (grep -c считает строки, не совпадения). Критерий
   выполним любым правильным решением.
4. **Говорящий провал:** при неудаче печатать, какое условие не выполнено, с
   фактическими значениями (`FAIL: bots=3 moved=false`). Одинокие FAIL/ERROR
   запрещены. Отчёт читает LLM-кодер — пишется для него.
5. **Симулированные проверки запрещены:** команда не имеет права безусловно печатать
   expect. Нельзя проверить одной командой — задача режется иначе, но проверка
   не подменяется имитацией.
6. **Серверный шаблон:** `node --check` до запуска; сервер — дочерним процессом с
   пересылкой stdout/stderr (`SERVER:`-префикс) и кодом выхода; готовность —
   ретраями подключения до 3с, не фикс-паузой; kill и лимит 10с.
7. Все JS-критерии исполняются через временный **`.cjs`-файл внутри workspace**
   (checkRunner) — НИКОГДА shell-строкой (`$$` в bash портит `$$eval` до запуска
   Node; tmpdir не годится — require не дорезолвится до node_modules проекта).
8. **Предпроверка (капкан на пустышки):** каждый критерий тихо (stdio pipe, 5с)
   прогоняется на текущем workspace ДО работы кодера; проходит заранее → фиктивен →
   одна регенерация; лог одной строкой на задачу.
9. **Контракт регрессии** (класс type=regression, последняя задача каждого дерева):
   только повторный прогон УЖЕ СУЩЕСТВУЮЩИХ критериев done-задач (склейка id, не
   генерация нового кода проверки), исполняется тем же checkRunner; писать новые
   Playwright-скрипты регрессии запрещено (систематический источник битого
   синтаксиса в v2: #6, #15, #20 падали не по существу).
10. Контракт задачи целиком: измеримая цель, критерий = команда+ожидание, границы
    (что нельзя трогать), лимиты (итерации/бюджет), стоп-факторы.

---

## 9. Двойное тестирование

**Контур 1 — код (Checker):** тестер без LLM, отдельный чистый процесс, критерии
из журнала, таймаут, отчёты до 1000 символов (200 душили диагностику вложенных
процессов). Тесты — часть проверки, не единственная истина; финальная
ответственность — человек.

**Контур 2 — впечатление (обживатель):** после зелёной доски LOOM проходит продукт
как первый пользователь: открылось ли, понятно ли за 10 секунд, находится ли
главное действие, не ломается ли путь клиента. Итог — не вердикт, а пачка
tweak/polish-задач с обоснованиями («кнопка экспорта не видна без скролла на
375px»). Один цикл на сдачу. Именно отсюда берётся «продумано, почти без правок».

---

## 10. Исследователь и данные

Включается для problem-входов и по заказу архитектора («для решения нужны данные X»).

- **Ищет источники** (веб-доступ через своего провайдера), оценивает доступность,
  свежесть, покрытие.
- **Заказывает инструменты добычи конвейеру:** «нужен парсер площадки N» — обычная
  задача на доске (role=coder, criteria: «выдаёт ≥50 записей с полями марка/год/цена»).
  Рекурсия конвейера — штатный механизм.
- **Критерии данных** — свой класс проверок: свежесть (дата последней записи),
  объём (минимум N), правдоподобие диапазонов (цена авто ∉ {0, 10⁷$}).
- **Жесточайший стоп-фактор:** данных нет / добыть не удалось → честное
  blocked_needs_human с формулировкой «этих данных не существует в открытом виде»
  клиенту. Выдумать данные — худший провал системы, хуже любого падения.
- **Этика:** только публичные данные, уважение robots/ToS, без обхода защит.
- Продукт для problem-клиента формулируется честно: «LOOM собрал живые данные и дал
  инструмент видеть — решение принимает клиент», не «LOOM знает будущее».

---

## 11. Приём ТЗ-пакета (spec-вход)

Вход — папка/архив: ТЗ, доки, макеты, примеры. Пайплайн: разбор каждого документа →
единый root_spec → карта требований → **поиск дыр и противоречий в самом ТЗ**
(вопросы клиенту только по дырам, не по вкусам) → дерево задач с трассировкой
«требование → задача → критерий». Требование без критерия = дыра, о которой
сообщено клиенту до старта.

---

## 12. Безопасность

1. **Docker с ПЕРВОГО появления кодера с инструментами** (не фазой позже): смонтирован
   только workspace, наружу физически нельзя (инцидент: папка на рабочем столе).
   До Docker — временная решётка: замок пути (запись только внутри workspace) —
   обязательный минимум с первого дня.
2. Сеть контейнера: по умолчанию `--network none`; зависимости ставятся на этапе
   сборки образа из packages-плана архитектора; нужен пакет посреди задачи →
   статус «нужен пакет» → пересборка. Позже — egress-allowlist (registry.npmjs.org).
   Лимиты: `--memory`, `--cpus`, `--pids-limit`.
3. **Maker не трогает Checker:** allowlist (не blocklist) на защищённые файлы;
   grader в отдельном процессе (инцидент: подмена assert).
4. **gitGuard:** физическая защита репозитория самого LOOM от stray-веток агентов.
5. **Политика секретов:** ключи живут только в `.env` вне репо; в документах, ТЗ,
   скриншотах, коммитах — никогда (два реальных инцидента: nvapi-ключ на скриншоте,
   GitHub-ключи в теле ТЗ v3). Утёк — немедленный отзыв.
6. Инструменты агента не имеют доступа к journal.db и prompts/ (территория Checker).

---

## 13. Бюджеты и тиры

`budget_usd`/`spent_usd` на задаче: фактическая стоимость каждого вызова
прибавляется; проверка ДО следующего вызова; превышение → blocked_needs_human.
Второй уровень — бюджет проекта (координатор). Тиры: лёгкая модель по умолчанию,
дорогая — только когда дешёвая не справилась (лестница §7). Подписочный провайдер
(claude-cli) учитывается условной ценой для сопоставимости метрик.

---

## 14. LLM Ops / Evals

- **Уровень 1:** run_id-трейс с первого вызова; любой инцидент = SQL-запрос.
- **Уровень 2:** агрегаты поверх events: стоимость по ролям, % задач с первой
  попытки, файлы-конфликтогены, тренд стоимости задачи, латентность моделей.
- **Уровень 3:** LLM-as-judge (лёгкая модель) — оценка DAG архитектора после прогона
  (атомарность, дубли, реалистичность touches_files) → eval_score в events.
- **Eval интервью** (урок регрессии советника): банк эталонных желаний → прогон →
  автопроверка «нет технических вопросов клиенту» (лёгкой моделью: «есть ли в вопросе
  числа/алгоритмы/форматы?»). Промпт — тоже код, и ему нужны тесты.

---

## 15. Дрейф — три типа и лекарства

- **Context drift** (забывает начало к 15-му ходу) → §6: дельты + rolling summary.
- **Goal drift** (уезжает от ТЗ) → root_spec вне контекста + pre-work confirmation +
  перечитывание цели на каждой проверке.
- **Coordination drift** (параллельные агенты к несовместимым целям) →
  declares_interface (tsc --noEmit как level-0) + touches_files с реактивной
  перепроверкой.

---

## 16. Параллельность (после стабильного однопоточного режима)

- CODER_WORKERS=N, каждый в своём git worktree. Правило масштаба: параллелятся
  прежде всего задачи из разных зон дерева; 2–3 воркера — норма, 5 — потолок
  (структура зависимостей, а не железо — узкое место).
- **Sequential mergeQueue:** done → merge_pending → merge в main → rebase остальных.
- **Entity-level claiming:** touches_functions; два агента в одном файле допустимы
  на разных функциях (tree-sitter merge driver).
- **Конфликт после rebase:** авто-мёрж непересекающихся → LLM-reconciler (обе ветки +
  задачи; ~94% решается выбором существующего варианта, лёгкой моделью) → эскалация.
- **Fan-in:** failed блокирует done-ветки? да → проект blocked_needs_human;
  нет → продолжать, failed в retry-очередь.

---

## 17. Сдача: отчёт консультанта

Сдаётся не «вот файлы», а позиция:
«Твоя задача/проблема была — …; я принял решения — … и вот почему; данные взяты
оттуда-то (живые, обновляются так-то); проверено так-то (оба контура); вот что
требует твоего решения; вот что я бы сделал следующим шагом.»
Для wish-входа блок «проблема» опускается — только «что получилось и как проверено».

## 18. Maintenance после сдачи

Правки и развитие идут той же доской того же проекта (tweak/project). Автономные
изменения живого прода — только после многократно пройденного уровня Checker:
PR → CI в чистом окружении (не верить локальному вердикту агента) → staging +
health-период → прод; rollback одной командой; rate-limit автономных мёржей в сутки;
мониторинг прода порождает задачи regression_detected в журнал.

---

## 19. Диагностическое правило четырёх уровней

Баг повторяется после правки промпта → это НЕ Prompt-проблема. Уровни:
**Prompt** (что написано) → **Context** (что модель видит) → **Harness**
(инструменты, изоляция, fallback) → **Loop** (когда/как завершается цикл).
Чинить строго на правильном уровне. Канонический пример: submit_result не вызывался —
промпт не помог, решилось на Loop (tool_choice required + сужение списка).

---

## 20. Инварианты среды (нарушение = баг)

1. `--env-file=.env` во ВСЕХ npm-скриптах (не пакет dotenv).
2. `maxRetries: 0` в SDK.
3. Проект вне iCloud/облачной синхронизации (`~/dev/loom`).
4. Все JS-критерии — через `.cjs`-файл, никогда shell-строкой.
5. run_id в events с первого вызова модели.
6. Системный промпт claude-cli — только файлом; user — только STDIN
   (Windows режет аргумент по \n).
7. Секреты — только `.env`, никогда в документах/репо/скриншотах.
8. Кроссплатформенность явная: пути через node:path; bash-зависимости checkRunner
   документированы (Windows = Git Bash); БД — better-sqlite3 за тонким адаптером
   journal-интерфейса, fallback node:sqlite (нативная сборка на Windows падала).
9. Ввод сеанса санитизируется (инцидент: склейка терминального буфера в title задачи).
10. MOCK-ветка каждой фичи — ПОСЛЕ проверки реального пути, не до.

---

## 21. Реестр шрамов (обязателен к прочтению новыми разработчиками)

Каждая строка — реальный инцидент → механизм. Удалять механизм можно только вместе
с объяснением, почему шрам больше не актуален.

1. `.env` не грузился → инвариант --env-file
2. maxRetries:2 SDK утраивал таймауты → maxRetries:0, retry в шлюзе
3. Прозаический ответ принят как финальный → tool_choice required + retry на каждом ходу
4. Кодер пропускал git_commit → auto-commit safety net
5. Stray-ветка на репо LOOM → gitGuard
6. iCloud dataless node_modules → проект вне облачных папок
7. `$$` в shell-критериях ломал $$eval до Node → checkRunner, .cjs в workspace
8. llama: один tool_call за ход → MODEL_LIMITATIONS
9. currentFiles слал весь workspace (34К/ход) → дельта
10. Вымышленные ID моделей (404) → сверка с /v1/models, никакого хардкода по памяти
11. Кривой JSON модели убивал прогон → валидация внутри шлюза, не-JSON = сбой модели
12. Таймаут 180с убивал думающий DeepSeek (514с ответ) → 300с
13. «Все модели недоступны» в час пик → второй круг через 60с, длинные цепочки
14. Reasoning на «задай 3 вопроса» (минуты) → THINKING off для лёгких ролей
15. Советник спрашивал клиента «скорость в px» → product/engineering split + regex-фильтр КОДОМ
16. Бриф-галлюцинация из «continue» → guard мусорного входа + /resume
17. Слепой кодер переписал рабочую игру с нуля → «глаза» (текущие файлы в контексте)
18. module.exports убил браузерную игру → тип кода по задаче в промпте кодера
19. Критерий-пустышка «Simulated check» → запрет + предпроверка с регенерацией
20. Немой «BOTS_FAIL» — кодер чинил вслепую → говорящие провалы, отчёты 1000 символов
21. grep -c и точные числа валили правильный код → правило минимумов
22. Некому ставить ws/express → packages-план + npm install до кодера
23. stderr упавшего сервера терялся в критерии → серверный шаблон (SERVER:, node --check)
24. Уроки про Unity/Python в JS-проекте → фильтр галлюцинаций + SKIP
25. Задачи навечно в doing после смерти процесса → releaseStuck при каждом входе
26. Правка без отката похоронила проект → .history-бэкап перед каждым изменением + git
27. readline терял опережающий ввод → очередь строк в io
28. Windows обрезал CLI-аргумент по \n (промпт = одно слово) → system-prompt файлом, user через STDIN
29. --bare требовал API-ключ, игнорируя подписку → ручная изоляция headless (tools "", strict-mcp, settings)
30. chart.min.js в «глазах» жёг ~15К токенов/вызов → фильтр .min/размера + правило CDN
31. Регрессионные критерии систематически битые (#6, #15, #20) → контракт регрессии: только повтор существующих критериев
32. Терминальный мусор склеился в title задачи → санитизация ввода сеанса
33. Секреты в скриншоте и в теле ТЗ → политика секретов (§12.5)
34. Задвоенное экранирование regex в критерии архитектора (`/\\d/` вместо
    `/\d/`) сделало критерий НЕВЫПОЛНИМЫМ никаким кодом — кодер честно чинил
    рабочий UI 4 попытки подряд против сломанного теста, исчерпал бюджет и
    лестницу, дерево осталось недостроенным → детектор «одинаковый провал
    при разном коде» (git tree-хэш между попытками) + одноразовое ревью
    архитектора (agents/architect.js:reviewCriterion, coordinator.js:
    detectCriteriaDefect) чинит критерий (journal.js:repairCriteria) или
    честно подтверждает блок — доверие построено на недоверии и к
    собственным критериям тоже

---

## 22. Фазы реализации (реальный прогон в конце КАЖДОЙ фазы)

- **Фаза 0 — фундамент:** каталог вне облака; --env-file; БД-адаптер (better-sqlite3 /
  node:sqlite); схемы tasks+events (run_id, role, type); config с MODEL_LIMITATIONS;
  BEGIN IMMEDIATE/DEFERRED.
- **Фаза 1 — один агент по-настоящему:** claude-cli как единственный провайдер
  (Haiku лёгким ролям, Sonnet кодеру, Opus архитектору); JSON-контракт кодера
  (вариант А, §7-статус); maxRetries:0; grader автозапуском; **замок пути кодера с
  первого дня**; ПЕРВЫЙ РЕАЛЬНЫЙ ПРОГОН.
- **Фаза 2 — Context Engineering:** дельта-глаза + фильтр мусора; rolling summary;
  prompt caching (динамика в user); живой тест budget_usd.
- **Фаза 3 — роли и Router:** пять типов входа + протоколы интервью советника +
  regex-фильтр; эскалация tweak→project; role/type в claimNext; предпроверка
  критериев; контракт регрессии; blocked_by_task_id.
- **Фаза 4 — безопасность:** Docker для кодера (сеть none, лимиты); gitGuard;
  tsc-контракты level-0.
- **Фаза 5 — GitHub:** gh CLI (не App); PR-поток.
- **Фаза 5.5 — обживание:** live_in-агент → polish/tweak-задачи; отчёт консультанта
  при сдаче (§17).
- **Фаза 6 — параллельность:** worktrees, mergeQueue, touches_functions + weave,
  fan-in, reconciler.
- **Фаза 7 — глубина входа:** приём ТЗ-пакета (ingestion §11); Исследователь (§10)
  с критериями данных и стоп-факторами; eval интервью и LLM-as-judge DAG.
- **Фаза 8 — по потребности:** FTS5 skills; Telegram-вход; дашборд (три колонки:
  статусы/детали/чат с аналитиком); Maestro E2E с Mac-runner; maintenance-прод (§18).

---

## 23. Анти-скоуп (сознательно НЕ делаем сейчас)

Дообучение советника (готовые модели достаточны); векторный RAG до 100+ skills
(grep+SQL хватает); GitHub App на старте (gh CLI проще); Maestro/мобильное до Фазы 8;
собственный GPU-инференс; мультиарендность/чужие пользователи на подписочном
провайдере (только API-ключи, см. политику подписки); «смотритель 24/7» до
maintenance-фазы — починка по запросу через tweak.

## 24. MOCK-режим

Каждый механизм имеет MOCK-ветку (после реального пути): детерминированные ответы
ролей, сценарий с нарочной ошибкой кодера в первой попытке (демонстрация цикла
поймал→отчёт→починил), маркеры маршрутов router. `MOCK=1` — полный прогон без
единого вызова модели; обязательный смоук перед любым реальным запуском и после
любой правки машины.

## 25. .env и запуск
===================================================================================

# LOOM — §27 «Дизайн-контур» (ТЗ + порядок сборки)

> Статус: полноправный раздел ТЗ v4, продолжение после §26. Отдать этот файл
> Claude Code целиком; правила исполнения — в конце документа. Положить в
> `docs/` и закоммитить до начала работ.

---

## 27.0. Проблема и философия

Продукты LOOM функционально корректны и визуально бедны. Причина не в моделях,
а в архитектуре: **что не проверяется — того не существует** (дух §8), а
дизайн сейчас не существует трижды:

1. никто не принимает дизайн-решений — каждый кодер в каждой задаче изобретает
   стиль заново, между задачами разнобой, при параллельности (Фаза 6) два
   воркера красят один продукт в разные цвета;
2. дизайн никак не проверяется — критерии архитектора спрашивают «канвас
   существует», но не «отступы согласованы»;
3. никто не видит результат глазами — кодер слеп (вариант А, §7), тестер
   проверяет поведение, live_in смотрит UX-фрикции, но не эстетику.

Решение — не «роль художника» и не «пиши красиво» в промпте кодера, а триада,
зеркалящая существующую грамматику системы:

```
корректность:  архитектор определяет → кодер реализует → тестер проверяет
эстетика:      дизайнер   определяет → кодер реализует → обживатель смотрит
```

Дизайнер — законодатель, не исполнитель: он НЕ редактирует файлы продукта
(§3 сохраняется), он издаёт дизайн-систему, против которой дальше работают
критерии и глаза. Три дыры закрываются тремя этапами (A, B, C ниже), каждый
самостоятельно ценен, порядок сборки — по возрастанию цены.

## 27.1. Доменный тег: когда контур включается

Дизайн-контур работает только для продуктов с интерфейсом. Механика:

- советник (wish/problem/spec-протоколы) выставляет в брифе новое поле
  `domain: "ui" | "cli" | "lib" | "data"` (одно значение; правило в
  промптах советника: есть страницы/канвас/экраны → `ui`);
- harness страхует эвристикой: если советник поля не дал, но в root_spec /
  задачах встречаются html/canvas/страница/экран — домен `ui`
  (детерминированная проверка в talk.js, регистр учитывать по правилу
  кириллических регэкспов из DECISIONS — НЕ `\b`);
- домен пишется в root_spec и наследуется всеми задачами проекта.

`domain !== "ui"` → ни один механизм §27 не вызывается, ни один вызов не
тратится. Это инвариант стоимости.

## 27.2. Этап A — роль designer (закрывает дыру 1)

### Место в потоке

```
советник (бриф) → [domain=ui] дизайнер (система) → архитектор (DAG) → ...
```

Дизайнер вызывается ОДИН раз на проект, до архитектора — архитектору нужна
готовая система, чтобы вписать дизайн-критерии (этап B) и передать токены
кодеру. Для tweak-маршрута по существующему продукту дизайнер НЕ вызывается,
если `workspace/design/tokens.css` уже существует — система издаётся один раз
и дальше только соблюдается (повторное издание = разнобой, ровно то, с чем
боремся).

### Конфигурация

- `ROLES.designer = ['claude-cli:opus']` — сознательно сразу opus, без
  лестницы: один вызов на проект с максимальным рычагом (его выход входит
  в контекст ВСЕХ последующих UI-задач), экономить здесь — ложная экономия.
- `agents/designer.js` — по образцу других агентов: собирает user-контекст
  (бриф советника + root_spec + существующий tokens.css при tweak +
  дизайн-уроки из skills.db категории `design`), вызывает
  `callAgent({role:'designer', promptFile:'prompts/designer.md', json:true})`.
- `prompts/designer.md` — статичный системный промпт (§6.3: ни одной
  переменной внутри).

### Контракт выхода (строго JSON)

```json
{
  "direction": "2-3 предложения: характер продукта и выбранное настроение",
  "tokens_css": ":root { --color-bg: #0f1115; --color-surface: ...; --space-1: 4px; ... }",
  "design_md": "правила компоновки в markdown: сетка, иерархия, состояния, что запрещено",
  "fonts": [{"family": "system-ui", "role": "base"}],
  "lessons_hint": "необязательно: чему научился проект (для skills)"
}
```

Требования к `tokens_css` (проверяются детерминированно в harness ДО записи,
провал → одна повторная попытка, затем `blocked_needs_human` — дизайнер тоже
живёт по лестнице):
- только CSS custom properties внутри одного `:root {}` (и опционально
  `[data-theme=...] {}`), никаких селекторов продукта — токены, не стили;
- обязательные группы: `--color-*` (фон, поверхность, текст, primary, danger —
  минимум 5), `--space-*` (шкала 4/8-кратная, минимум 4 ступени), `--radius-*`,
  `--font-*`, `--shadow-*`;
- парсабельность: harness прогоняет через простой CSS-парс (без зависимостей:
  скобочный баланс + регэксп деклараций достаточно) — синтаксически битые
  токены не доезжают до кодера.

Harness (не модель) пишет `workspace/design/tokens.css` и
`workspace/DESIGN.md`, коммитит обычным autoCommit. Шрифты: только системные
стеки или явный Google Fonts `<link>` (без скачивания файлов — сеть у кодера
не предполагается).

### Доставка кодеру

В user-сообщение кодера для каждой задачи UI-проекта harness добавляет блок
«Дизайн-система проекта»: полный tokens.css + DESIGN.md (они короткие — это
закон, а не библиотека). В `prompts/coder.md` — новая СТАТИЧНАЯ секция:
при наличии дизайн-системы весь CSS обязан использовать `var(--...)` из неё;
захардкоженный hex/px там, где есть токен — брак, который поймает критерий.

## 27.3. Этап B — класс дизайн-критериев (закрывает дыру 2)

Расширение мандата архитектора, НЕ новое правило вместо §8.3 — правило
минимумов сохраняется полностью, минимумы просто становятся дизайнерскими.

В `prompts/architect.md` — статичная секция «Дизайн-критерии (domain=ui)»:
каждая задача, создающая или меняющая видимый интерфейс, обязана нести
МИНИМУМ два критерия из этого класса (в дополнение к функциональным):

1. **Подчинение токенам:** computed styles через Playwright — использованные
   на странице цвета ⊆ множества значений `--color-*` токенов (сравнение
   после нормализации в rgb; допуск: прозрачности от токен-цветов);
2. **Шкала отступов:** margin/padding ключевых контейнеров кратны базовой
   ступени `--space-1` (минимум: ни одного «голого» значения, не кратного
   базе, на проверяемых элементах);
3. **Типографика:** ≤ 2 font-family на странице; body font-size ≥ 14px;
4. **Контраст:** цвет текста к фону ≥ 4.5:1 для основного текста (формула
   WCAG — двадцать строк чистого JS в критерии, без зависимостей);
5. **Тач-таргеты:** интерактивные элементы ≥ 44×44 px в мобильном вьюпорте
   375px (вьюпорт уже стандарт live_in, §9).

Механика — обычные `{cmd, expect}`: `.cjs` через checkRunner (шрам 7),
Playwright в `packages` задачи (архитектор уже умеет заказывать пакеты).
**Фолбэк без Playwright** (браузеров нет — реальный случай из аудита):
критерий обязан деградировать в статическую проверку (CSS файла продукта
парсится, проверяется использование `var(--...)` и отсутствие захардкоженных
hex там, где есть токен) с честной пометкой в отчёте «проверено статически,
без рендера». Шаблон обоих вариантов — в промпте архитектора, чтобы он не
изобретал деградацию сам.

Предпроверка §8.8 действует: дизайн-критерий, зелёный на пустом workspace —
фиктивен, регенерация.

## 27.4. Этап C — визуальное ревью (закрывает дыру 3)

Расширение live_in (§9, контур 2), НЕ новая роль — обживатель уже ходит по
продукту как первый пользователь, теперь он получает глаза в буквальном
смысле.

### Механика

1. После существующего осмотра live_in, для `domain=ui` при `DESIGN_REVIEW=1`
   (env, по умолчанию 0 — это самый дорогой механизм системы): Playwright
   снимает два скриншота — 1280×800 и 375×667 — в
   `workspace/.loom-shots/` (каталог в .gitignore продукта);
2. один вызов `callAgent({role:'live_in', promptFile:'prompts/livein.review.md'})`
   с изображениями; контракт выхода — как у текущего live_in: список
   tweak-задач `{title, reason, severity}`, но про визуальное: иерархия,
   выравнивание, пустоты, состояния наведения/фокуса, соответствие
   direction'у дизайнера. Ограничение против фонтана вкусовщины: максимум
   3 задачи, каждая с конкретным наблюдением («кнопки в шапке прилипли к
   краю, нет --space-3»), задачи без конкретики harness отбрасывает
   (детерминированный фильтр: reason короче 40 символов → в мусор);
3. созданные задачи — обычные tweak в общей лестнице и бюджете.

### Передача изображений: контролируемое отступление от «голого мозга»

Гейтвей сейчас не умеет изображений, и claude CLI в headless-режиме LOOM
работает с отключёнными инструментами. Расширение:

- `callAgent` получает опциональный параметр `imagePaths: [абс. пути]`;
- при его наличии gateway добавляет к вызову CLI разрешение ТОЛЬКО на чтение
  и ТОЛЬКО каталога скриншотов (механику выяснить по факту у установленного
  CLI: `--allowedTools` с ограничением пути / permissions в
  `.loom-tmp/settings.headless.json` — выбрать самый узкий работающий
  вариант и зафиксировать выбор в DECISIONS), а пути файлов вписывает в
  user-текст;
- **инвариант:** ни один другой вызов (кодер, архитектор, советник, …)
  не получает никаких инструментов — отступление действует ровно для вызова
  с `imagePaths`, каждый такой вызов логируется событием
  `image_call` (role, файлы, размер);
- **деградация:** передача изображений не заработала (старый CLI, ошибка
  разрешений) → ревью выполняется по DOM-выжимке (существующий осмотр
  live_in + computed styles сводка) с честной пометкой `visual:false` —
  контур слабеет, но не умирает и не притворяется зрячим.

## 27.5. Уроки дизайна

skill_writer после сдачи UI-проекта получает в контекст также direction
дизайнера и принятые/отклонённые polish-задачи; уроки пишутся в skills.db
с категорией `design` («тёмная тема: elevation поверхностями, не бордерами»).
Дизайнер следующего проекта получает топ дизайн-уроков в user-контекст —
вкус системы накапливается тем же механизмом, что инженерные шрамы.

## 27.6. MOCK-покрытие (§24 — обязателен для каждого механизма)

Без единого вызова модели и без браузеров:

1. **мок-дизайнер:** детерминированные валидные токены; сценарий wish с
   domain=ui → tokens.css/DESIGN.md появились в mock-workspace, попали в
   user-текст мок-кодера (проверить подстроку), второй tweak-заход дизайнера
   НЕ вызывает (файл уже есть);
2. **валидация токенов:** мок-дизайнер отдаёт битые токены (селектор вместо
   :root) → harness ловит до записи, одна повторная попытка, затем
   blocked_needs_human;
3. **дизайн-критерии:** задача с критерием-деградацией (статическая ветка —
   она не требует браузера): мок-кодер первой попыткой хардкодит hex →
   FAIL с говорящим отчётом; второй — использует var(--...) → PASS;
4. **ревью-маршрут:** DESIGN_REVIEW=1 в MOCK → скриншот подменяется
   фикстурным PNG-файлом (1×1, лежит в evals/fixtures/), мок-live_in
   возвращает 2 валидных + 1 бессодержательную задачу → harness создал 2,
   отбросил 1, событие image_call записано; проверяется маршрут и контракт,
   не «зрение»;
5. **домен-гейт:** сценарий с domain=cli → ни дизайнер, ни ревью не
   вызывались (по events).

MOCK-изоляция путей — закон (`.loom-shots` в mock-workspace, ничего в
реальных каталогах).

## 27.7. Что сознательно ЗА рамками §27 v1

Темизация под бренд клиента из интервью (советник мог бы спрашивать про
фирменные цвета — отдельное решение), генерация иллюстраций/логотипов,
скриншот-дифф между попытками, дизайн-ревью КАЖДОЙ задачи (только сдача),
Figma-вход. Двери открыты, сейчас не строим.

---

# Порядок сборки и правила исполнения (для Claude Code)

Read first: `docs/LOOM_TZ_v4.md` (§3, §6.3, §8, §9, §21), `docs/DECISIONS.md`,
this file. Skim `agents/livein.js`, `agents/architect.js`, `core/engine.js`,
`core/gateway.js`, `core/mock.js` before editing.

**Build order — one commit per stage, `npm run mock` + `npm run race` green
before every commit:**

1. **Stage A** (27.1 + 27.2): domain tag (advisor prompts + harness heuristic
   in talk.js), `agents/designer.js`, `prompts/designer.md`, ROLES entry,
   token validation in harness, tokens delivery to coder user-text, static
   coder.md section, MOCK scenarios 1/2/5. Commit: `phase 9a: designer role +
   design tokens (§27.2)`.
2. **Stage B** (27.3): architect.md static section with both criterion
   templates (Playwright + static fallback), MOCK scenario 3. Commit:
   `phase 9b: design criteria class (§27.3)`.
3. **Stage C** (27.4): `imagePaths` in callAgent/gateway (narrowest working
   CLI permission mechanism — investigate the installed CLI's flags, pick the
   most restrictive, document in DECISIONS), screenshots in livein,
   `prompts/livein.review.md`, deterministic reason-filter, `DESIGN_REVIEW`
   env (default 0), degradation path `visual:false`, `image_call` event,
   MOCK scenario 4. Commit: `phase 9c: visual review contour (§27.4)`.
4. skills category `design` (27.5) — fold into whichever stage touches
   skill_writer, or a small fourth commit.

**Hard rules (same law as always):** system prompts stay byte-static, all
dynamic content in user messages; all model calls via engine/gateway only; no
real model calls and no network in this session — everything verified through
MOCK, real-run steps appended to `docs/RUNBOOK.md` («§27: реальный UI-прогон с
DESIGN_REVIEW=1», «проверка image-передачи на живом CLI»); paths via
node:path, writes only under workspace; feedback overwritten never
accumulated; Cyrillic regexes without `\b`; no secrets anywhere; ambiguity →
safest interpretation + DECISIONS entry, never stop to ask; do not refactor
gateway spawn quirks — extend around them.

**Definition of done:** all five MOCK scenarios green inside `npm run mock`;
`grep` proves no tool permissions on any non-imagePaths call path; DECISIONS
has a dated «§27» section (incl. the CLI image mechanism chosen and why);
RUNBOOK appended; README «Что построено» updated; final report — what was
built per stage, DECISIONS entries, RUNBOOK items awaiting the human.

Begin with Stage A now.


================================================================================

# LOOM — §28 «ТЗ-писатель» (ТЗ + порядок сборки)

> Статус: полноправный раздел ТЗ v4, продолжение после §27. Отдать этот файл
> Claude Code целиком; правила исполнения — в конце. Положить в `docs/` и
> закоммитить до начала работ.

---

## 28.0. Проблема и философия

Рабочий процесс автора сейчас выглядит так: желание → отдельный чат с сильной
моделью → подробное ТЗ руками туда-обратно → только потом ТЗ отдаётся
исполнителю. Причина проверена практикой: модель, пишущая ТЗ в спокойном
диалоге, делает это лучше, чем модель, которой сказали «сделай» — она
вынуждена сначала ПОДУМАТЬ письменно, и все дальнейшие решения меряются об
этот документ, а не изобретаются на ходу.

LOOM обязан содержать эту петлю внутри себя. Сейчас между брифом советника
(компактный: summary + engineering_defaults) и DAG архитектора нет
промежуточного артефакта — архитектор декомпозирует сразу из брифа, и всё
недосказанное в брифе достраивается молча. §28 вставляет недостающий шаг:

```
советник (интервью → бриф)
   → ТЗ-писатель (полное ТЗ документом)
      → утверждение человеком (петля правок)
         → разбор ТЗ существующим ingest-механизмом (§11: requirements, дыры)
            → [domain=ui] дизайнер (§27)
               → архитектор (DAG с covers по требованиям ЭТОГО ТЗ)
```

Ключевой принцип: **не строить параллельный путь**. Фаза 7 уже умеет
принимать внешние ТЗ-пакеты — извлекать требования, находить дыры, требовать
от архитектора трассировку `требование → задача` (covers). §28 не дублирует
эту машину, а ставит перед ней внутреннего автора: ТЗ, написанное самим LOOM,
проходит ровно тот же приёмный конвейер, что ТЗ, принесённое клиентом. Один
механизм — два входа.

## 28.1. Когда шаг включается

- Маршруты `wish` и `problem` — всегда, при `TZ_MODE=1` (env, **по умолчанию
  1**: это теперь штатный путь создания проектов; `TZ_MODE=0` — сознательный
  пропуск для одноразовых мелочей).
- Маршрут `problem`: ТЗ-писатель вызывается ПОСЛЕ researcher'а (§10) — ТЗ,
  написанное до оценки доступности данных, будет фантазией; вердикт
  `data_unavailable` останавливает проект до всякого ТЗ.
- Маршруты `tweak` и `question` — никогда (точечная правка не переписывает
  конституцию продукта; но см. 28.5 про дополнение ТЗ).
- Внешний ТЗ-пакет уже принесён (`/ingest`) — ТЗ-писатель не вызывается:
  автор ТЗ уже существует, это клиент. Оба флага одновременно — ingest
  побеждает, событие в лог.

## 28.2. Роль spec_writer

- `ROLES.spec_writer = ['claude-cli:opus']` — сознательно сразу opus без
  лестницы, та же логика, что у дизайнера (§27.2): один вызов на проект,
  выход которого становится законом для всех последующих ролей; именно
  качество этого текста — причина существования §28.
- `agents/specwriter.js` по образцу остальных: вход — бриф советника,
  root_spec, полная стенограмма интервью (советник спрашивал — ответы
  человека не должны потеряться по дороге в ТЗ), для problem-маршрута —
  `for_architect`-блок researcher'а с данными, топ уроков skills.db.
- `prompts/specwriter.md` — статичный (§6.3), НИ ОДНОЙ переменной.

### Контракт выхода (строго JSON)

```json
{
  "tz_md": "полный текст ТЗ в markdown",
  "requirements": [
    {"id": "REQ-1", "text": "одно проверяемое требование", "priority": "must|should"}
  ],
  "open_questions": ["вопросы, на которые в интервью не прозвучало ответа"]
}
```

Обязательная структура `tz_md` (перечислена в промпте; harness проверяет
детерминированно по заголовкам — отсутствие секции = одна повторная попытка,
затем `blocked_needs_human`, лестница как у всех):

1. Цель и пользователи (кто и зачем);
2. Функциональные требования — нумерованные REQ-N, дословно совпадающие с
   массивом `requirements` (harness сверяет id и тексты: расхождение
   markdown-документа с машинным массивом = брак, требования — не украшение
   текста, а его источник);
3. Нефункциональные требования (производительность, мобильность, офлайн —
   что применимо);
4. Экраны/сценарии (для domain=ui) или интерфейс вызова (для cli/lib);
5. Данные (структуры, откуда берутся — для problem-маршрута сюда дословно
   входит вердикт researcher'а);
6. Приёмка уровня продукта (как клиент поймёт, что готово — человеческим
   языком, это НЕ критерии §8, те останутся за архитектором);
7. За рамками (анти-скоуп: что сознательно НЕ делаем — обязательная секция,
   пустой она быть не может: ТЗ без границ — не ТЗ);
8. Открытые вопросы (= `open_questions`).

Правила содержания в промпте: требования проверяемы и атомарны; никаких
технологий, уже решённых в engineering_defaults, не передумывать; ничего не
выдумывать сверх интервью — недосказанное идёт в open_questions, а не
достраивается фантазией (тот же грех, что симулированные проверки §8.5).

## 28.3. Утверждение человеком (петля правок)

ТЗ — единственный артефакт LOOM, который человек утверждает ДО начала трат
на реализацию (сейчас человек впервые видит результат уже после прогона).
Механика в talk.js:

1. harness пишет `workspace/docs/TZ.md`, печатает его целиком + отдельно
   open_questions;
2. приглашение: `утверждить / правка: <текст> / отмена`;
3. `правка:` → второй вызов spec_writer: прежнее ТЗ + текст правки + указание
   «внеси ТОЛЬКО запрошенное, требования, которых правка не касается, оставь
   дословно» (harness сверяет: id нетронутых требований не изменились —
   защита от тихой перегенерации, прямой урок бага №2 первого прогона);
   правок может быть несколько подряд, каждая — один вызов;
4. `утверждаю` → root_spec.requirements = массив требований, root_spec
   получает `tz_approved_at`, дальше конвейер;
5. `отмена` → проект закрывается честно, потрачено только интервью + 1-2
   вызова ТЗ (это и есть экономика §28: дешёвое «нет» до дорогой реализации);
6. неинтерактивные входы (Telegram, будущее API): open_questions отправляются
   как сообщение, утверждение — ответным словом; таймаут утверждения не
   существует — проект просто стоит в `awaiting_tz_approval` (новый статус
   проекта, не задачи), resume его подхватывает.

## 28.4. Стыковка с существующей машиной (переиспользование, не дублирование)

- После утверждения harness вызывает СУЩЕСТВУЮЩИЙ разбор Фазы 7 в режиме
  «требования уже извлечены»: пропускается только извлечение (advisor.spec
  по документам), а проверка дыр/противоречий на полном наборе требований —
  выполняется (свежий взгляд advisor.spec на собственное ТЗ системы ловит
  противоречия, которые автор-writer не видит — два разных промпта, две пары
  глаз, тот же принцип недоверия, что кодер/тестер);
- архитектор получает требования с ID и обязан заполнять `covers` — правило
  уже существует (Фаза 7), §28 лишь делает его действующим для ВСЕХ проектов,
  не только пришедших с внешним ТЗ; требование без покрывающей задачи —
  дыра, о которой сообщается до старта;
- дизайнер (§27) читает утверждённое ТЗ (секции 1, 4) вместо голого брифа;
- отчёт консультанта (§17) получает новую строку: сколько требований ТЗ
  закрыто (`covers` → done), со ссылкой на TZ.md в составе продукта —
  клиент получает не только код, но и документ, по которому код можно
  проверить;
- `TZ.md` живёт в workspace → уходит в git продукта autoCommit'ом и в
  GitHub-публикацию (Фаза 5) — ТЗ является частью поставки.

## 28.5. Tweak существующего продукта с ТЗ

Если у продукта есть `docs/TZ.md` и tweak затрагивает требования (harness
определяет дёшево: маршрутизатор уже классифицировал tweak; проверка —
упоминает ли правка что-то из текстов requirements, регэксп-поиск по
нормализованным словам, кириллица без `\b`):
- один вызов spec_writer в режиме «дополнение»: секция «Изменения» в конец
  TZ.md (`### <дата>: <суть>` + изменённые/новые REQ-N), нетронутые
  требования — дословно (та же сверка id, что в 28.3.3);
- без утверждения человеком (tweak сам по себе — воля человека), но
  напечатать дифф требований.
Накопительный принцип root_spec (фикс первого прогона) распространяется на
ТЗ: документ дополняется, никогда не перегенерируется целиком.

## 28.6. MOCK-покрытие (§24)

Без вызовов моделей:
1. wish при TZ_MODE=1 → мок-writer отдаёт детерминированное ТЗ (3 REQ, все
   секции) → мок-утверждение (в MOCK интерактив автозаменяется на
   «утверждаю» — как уже сделано для интервью) → requirements в root_spec →
   мок-архитектор покрывает REQ-1/REQ-2, REQ-3 нарочно без covers → дыра
   найдена и напечатана;
2. петля правок: мок-ввод «правка: добавь тёмную тему» → второй мок-вызов →
   REQ-4 добавлен, id и тексты REQ-1..3 неизменны (сверка проходит);
   негативный кейс: мок-writer «случайно» меняет текст REQ-2 → harness
   ловит, повторная попытка;
3. структурный брак: мок-writer без секции «За рамками» → повтор → снова без
   → blocked_needs_human;
4. TZ_MODE=0 → spec_writer не вызывался (по events), поток прежний;
5. tweak-дополнение (28.5): секция «Изменения» дописана, REQ-1..3 дословны;
6. problem-маршрут: writer вызван ПОСЛЕ мок-researcher'а, вердикт
   data_unavailable → writer НЕ вызывался вовсе.

MOCK-изоляция путей — закон.

## 28.7. За рамками §28 v1

Версионирование ТЗ как отдельных файлов (git продукта уже даёт историю),
согласование ТЗ несколькими людьми, экспорт в PDF/DOCX, автоматическая
регенерация критериев архитектора при правке ТЗ задним числом (сейчас —
только вперёд, через tweak). Двери открыты.

---

# Порядок сборки и правила исполнения (для Claude Code)

Read first: `docs/LOOM_TZ_v4.md` (§6.3, §8.5, §11, §17, §21),
`docs/LOOM_DEV_GUIDE_PART2.md` П§6.2 (ingest — что переиспользуем),
`docs/DECISIONS.md`, this file. Skim `bin/talk.js` (маршруты, интервью,
mock-автоответы), `agents/ingest.js`, `agents/advisor.js`, `core/mock.js`.

**Build order — one commit per stage, `npm run mock` + `npm run race` green
before every commit:**

1. **Stage A** (28.2): `agents/specwriter.js`, `prompts/specwriter.md`,
   ROLES entry, deterministic output validation (sections by headers,
   requirements↔markdown consistency check), TZ.md write. MOCK scenario 3.
   Commit: `phase 10a: spec_writer role + TZ contract (§28.2)`.
2. **Stage B** (28.1 + 28.3): TZ_MODE env (default 1), flow insertion in
   talk.js for wish/problem (problem — strictly after researcher verdict),
   approval loop (утверждаю/правка/отмена) with untouched-REQ verification,
   `awaiting_tz_approval` project state + resume pickup, MOCK scenarios
   1/2/4/6. Commit: `phase 10b: TZ approval loop (§28.3)`.
3. **Stage C** (28.4 + 28.5): hook into existing Phase-7 holes check
   (skip-extraction mode), covers enforcement for all TZ_MODE projects,
   designer/§27 reads TZ, consultant report line (covered/total REQ),
   tweak-append mode with diff print, MOCK scenario 5. Commit:
   `phase 10c: TZ wired into ingest/covers/report (§28.4-5)`.

**Hard rules (the standing law):** system prompts byte-static, dynamic content
in user messages only; all model calls via engine/gateway; no real model or
network calls this session — MOCK only, real-run steps appended to
`docs/RUNBOOK.md` («§28: реальный wish с TZ_MODE=1 — утвердить ТЗ руками,
затем правка, затем полный прогон до отчёта»); feedback overwritten;
paths via node:path, writes only under workspace; Cyrillic regexes without
`\b`; MOCK state fully isolated; do not duplicate ingest logic — refactor its
requirements/holes step into a reusable function if needed (that refactor must
keep the existing ingest MOCK scenario green untouched); ambiguity → safest
interpretation + DECISIONS entry, never stop to ask.

**Definition of done:** all six MOCK scenarios green inside `npm run mock`;
existing ingest and bank scenarios still green (no regression from the
refactor); DECISIONS has a dated «§28» section; RUNBOOK appended; README
updated; final report — stages built, DECISIONS entries, RUNBOOK items
awaiting the human.

Begin with Stage A now.