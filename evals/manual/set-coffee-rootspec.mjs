// One-off script (§29 cleanup): write the extracted coffee-only root_spec
// for the newly created «Пульс кофейни» project. Not a new core API — just
// a call to the existing setRootSpec() with hand-picked, verified-coffee-only
// content extracted from legacy's merged root_spec.
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { setRootSpec, getRootSpec } from '../../core/journal.js';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const spec = fs.readFileSync(path.join(__dirname, 'coffee-spec.txt'), 'utf8').trim();

const defaults = [
  'Критерии аномалий: (1) обвал цены — товар продан < 70% от цены pricelist (потеря маржи); (2) всплеск объёма — дневной объём товара > среднего дневного + 2σ; (3) простой — день с нулевыми продажами точки или суммой < 5000 (условный минимум). Пороги 70% и 2σ — стандартные для обнаружения выбросов, легко переконфигурировать.',
  'Дашборд показывает: выручка сети (сегодня/неделя/месяц/всё время), выручка каждой кофейни отдельно (столбцы или карты), график выручки по месяцам (линия, 12 точек), динамика за последний месяц покупнику (день в день)',
  'Товары: таблица топ-30 по выручке (название, количество, выручка, процент от общей), сортировка по выручке убывание, экспорт в CSV опционально',
  "Аномалии: хронологический список (дата, кофейня, товар, тип аномалии, значение, расхождение от нормы) с коротким объяснением (например: 'Цена 850сом вместо 1200сом от prricelist'), фильтр по типу и точке",
  'Качество данных: счётчики (всего строк, дубликаты, JSON-брак, восстановленные цены из pricelist, возвраты как отрицательный qty), таблица с примерами проблемных строк',
  'aggregates.json: структура {stats: {total_revenue, by_shop, by_month}, top_products: [...], anomalies: [...], data_quality: {...}}, генерируется один раз при загрузке и кэшируется в localStorage для быстрой перезагрузки',
  'Адаптивность: Дашборд одноколонный на мобилях < 768px, графики масштабируются, таблицы товаров и аномалий скроллируются горизонтально',
  'Визуально: светлый минималистичный дизайн (белый фон, тёмный текст), графики — простые линии и столбцы, акцент красный для аномалий, палитра из трёх цветов для трёх кофеен',
  'Никаких внешних зависимостей на сервер — все данные обрабатываются в браузере (FileReader API + встроенная обработка данных)',
  'Загрузка данных: file input + drag-and-drop для выбора трёх JSONL (sales-*) и одного JSON (pricelist) файла одновременно',
  'Обработка в браузере: FileReader API с построчным парсингом JSON Lines для потоковой обработки без блокировки UI',
  'Очистка данных: удаление дубликатов по (id, shop, ts), пропуск JSON-брака, восстановление цен из pricelist для пропущенных SKU, обработка возвратов как qty < 0',
  'Агрегация: aggregates.json структуры {stats, top_products, anomalies, data_quality} генерируется в браузере, кэшируется в localStorage с хешем исходных данных для инвалидации',
  'Четыре вкладки: Дашборд (карточки выручки + графики столбцы/линия по месяцам), Товары (таблица top-30), Аномалии (список хронологический с фильтрами по типу и кофейне), Качество (счётчики дублей/брака + примеры проблемных строк)',
  'Графики: Canvas API нативный (без Chart.js для офлайн-лёгкости), простые столбцы и линии',
  'Стек: Vue 3 Composition API, HTML таблицы, собственные компоненты, никаких heavy dependencies',
  'Адаптивность: одноколонный макет на < 768px, таблицы с горизонтальным скроллом на мобиле, Canvas масштабируется',
  'Дизайн: белый фон, тёмный текст, sans-serif, три цвета для кофеен, красный акцент для аномалий, минималистичный стиль',
  'UX: прогресс-бар при загрузке и обработке файлов, сообщения об ошибках парсинга, показ числа обработанных строк, кнопка перезагрузить данные',
];

setRootSpec('пульс-кофейни-2a9a1a', spec, defaults, 'wish', 'ui');
const r = getRootSpec('пульс-кофейни-2a9a1a');
console.log('spec length:', r.spec.length);
console.log('defaults count:', JSON.parse(r.engineering_defaults).length);
console.log('route:', r.route, 'domain:', r.domain);
